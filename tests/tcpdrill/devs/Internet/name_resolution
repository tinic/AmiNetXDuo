# NO NAMESERVER LINE.  The drill's interface is a static 10.9.9.1 on a tap
# device the harness creates itself, with nobody else on the subnet and no
# route off it, so a name server named here can never be reached.  It carried
# fs-uae's SLIRP forwarder, 10.0.2.3, which exists on no segment this harness
# has ever run on; see tests/netstack/devs/Internet/name_resolution for what
# that cost the harnesses that do look names up.
domain localdomain
