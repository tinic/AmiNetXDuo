# tests/perf/run-lossgate.sh baseline.
# NAME  DIRECTION  VALUE  TOLERANCE_PERCENT
# Recorded with 5% peer-to-guest loss, 4096 KB, 9 reps.
# Direction: rx
# Impairment: delay 0ms jitter 0ms reorder 0%
# Read and write are separate on purpose: the 0.16.6 regression
# moved them in opposite directions.
# Tolerance: twice the interquartile spread over root(reps), floor 15%.
#
# RE-RECORDED 2026-09-06.  The read floor it replaced was 66.0 against a tree
# delivering 314..385 at these same parameters, so the gate could not catch a
# receive regression under loss short of a fivefold collapse.  Verified
# achievable before it landed: the new floor is 385 less 24.3%, or 291, and
# three independent nine-arm runs on this tree read 314, 366 and 385.
#
# WRITE DID NOT MOVE: 2,139.0 before, 2,138.0 now.  Only read changed, by 5.8x,
# which is what a campaign that has been optimising RECEIVE should produce and
# is independent corroboration that transmit here is the rig's ~4.5 ms
# acknowledgement round trip rather than anything this tree does.
#
# The old read tolerance was 15% and this rig cannot hold read to 15%: the
# harness refused an earlier attempt at three reps for +-27.1% against its own
# 25% ceiling, which is how the mismatch surfaced.  Nine reps brings it to
# 24.3% and that is what a read regression now has to exceed.  dropped_rx is
# noisier still at 41.7%; treat it as a smoke alarm, not a gauge.
dropped_rx     lower          8.0   41.7
read_kbs       higher       385.0   24.3
retransmitted  lower          0.0   15.0
write_kbs      higher      2138.0   15.0
