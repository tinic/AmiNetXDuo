| GENERATED by src/net68k/CMakeLists.txt.  Do not edit this file, edit the
| template.
|
| One CPU class of one file, in a translation unit of its own with a name of
| its own.  Both halves of that matter.  The variants share local label names,
| so two in one translation unit is a duplicate-symbol error.  The object file
| name is what tools/profiler/profreport.py resolves a sample address through,
| by way of the linker map, so four members called n68k_checksum.S.obj in one
| archive make every one of them read as whichever the map lookup found.  The
| result is a profile that names the wrong routine, and not a build failure.
|
| SPDX-License-Identifier: MIT

#define N68K_MV 40
#include "n68k_checksum.S"
