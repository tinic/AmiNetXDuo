| GENERATED by src/crypto68k/CMakeLists.txt.  Do not edit; edit the template.
|
| One CPU class of one file, in a translation unit and a file name of its own.
| src/net68k/n68k_mv.S.in says why both matter, and the second reason is the
| one that is easy to miss: the object FILE name is how
| tools/profiler/profreport.py gets from a sample address to a symbol, so two
| archive members with one name make a profile name the wrong variant.
|
| SPDX-License-Identifier: MIT

#define @C68K_MV_DEFINE@ 1
#define C68K_MV_SUFFIX @C68K_MV_SUFFIX@
#include "@C68K_MV_SRC@"
