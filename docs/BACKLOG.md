# Backlog
What is outstanding. Nothing else — finished work leaves a commit message and a
comment beside the code, not an entry here.
**HARD CAP: 250 LINES.** Over it, delete rows. Never relocate them; git has it.
| Item | Why it is open | Cite |
|---|---|---|
| The emulator workflow runs FS-UAE and is red | This tree's rule is Amiberry only — FS-UAE needs an X server and Xvfb is not the fix — yet the one CI arm that boots a guest is an FS-UAE job, and it was already failing before the console merge. Either port it to Amiberry or stop pretending it gates anything | `.github/workflows/emulator.yml:49,138,151` |
| `console-probe.py --reconnect` fails on main | Exit 1 twice against an instance built from main, passing twice on a byte-identical branch tree. Sessions drift ~700 px of 163840; failing runs showed 2 geoms in the refresh loop where passing runs showed 5. Unconfirmed hypothesis: the Workbench was not idle on a fresh boot, so the drift is real screen change rather than decode error. Diagnosis was cut short | `tests/tools/console-probe.py` |
| `httpd -v` from a Shell on Workbench stalls the whole server while a mouse button is held | AmigaDOS console writes go through a handler that wants the layer lock, which Intuition holds for the duration of a drag or a menu. The standing launcher sidesteps it by logging to a file; making httpd itself immune means changing how it logs. Affects every endpoint, not just `/console` | `src/tools/httpd.c` |
| `ami_udivdi3.c` fails the 68060 whole-tree build | gcc emits `extb.l` and `tst.l aN` and the assembler is invoked for 68000. Reproduces on main; `anxnet.device` itself builds clean for 68060. Invisible because that target is not in `CROSS_CONFIGS`, so CI never builds it — same blind spot that hid the profiler arm | `src/common/ami_udivdi3.c` |
| Five `S2EVENT_*` are accepted and never posted | `S2EVENT_ERROR`, `_TX`, `_RX`, `_BUFF` and `_HARDWARE` are taken in the mask and no site ever emits them, so an application waiting on one waits forever. `cnet.device` posts from eight sites. Separately `S2_PacketFilter` stores `op_Filter` and never invokes it, which is a silent contract violation | `src/netdev/netdev_cmds.c:688-691`, `src/netdev/netdev_device.c:1373` |
| PCMCIA parity stops short of two things | Walking every `CISTPL_CFTABLE_ENTRY` instead of taking the first would plausibly reach the seven multifunction ethernet+modem cards `cnet.device` itself cannot drive — inference, needs one such card. The interrupt work (status-change handler, `CARDF_POSTSTATUS` on V39, the Gayle edge-latch clear on V37, and whether COR bit 6 should request level mode at all) must land as one change and cannot be tested here: Amiberry models neither the Gayle latch nor a contended socket | `src/netdev/netdev_pcmcia.c`, `src/netdev/dp8390.c` |
| The console records `.pfs` and nothing else can read it | An MP4 export would make a recording shareable. Two constraints decide the codec: the page is self-contained under a CSP that blocks every external fetch, so a muxer must be vendored or avoided; and these are palette screens, where a lossy codec destroys one-pixel text and the export stops being worth having. `VideoEncoder` (WebCodecs) takes per-frame timestamps, which is what the container now carries, so variable frame timing maps straight across. Lossless H.264 4:4:4, or WebM via `MediaRecorder` if the container matters less than the effort. Not for V1 | `src/tools/web/client/console/pfs.ts` |
| A skipped arm renders as a green PASS in the GitHub summary | `ci-arm.sh:117-123` maps exit 0 to `status=pass` whether the stage ran or skipped, which is why the cardsweep masking was total rather than amber. Changing it changes every arm's reporting | `tools/ci-arm.sh:117-123` |
| Four harnesses skip their only off-box assertion and exit 0 | Green with the substance untested | `run-dnscache.sh:190`, `run-routes.sh:270`, `run-iperf.sh:631`, `run-tcpdrill.sh:75` |
| `test-verdict.sh` reports 'skipped' over real failures | It greps for the string SKIPPED and returns 77 before checking the failure count. Still exits non-zero, so red — a wrong message, not a hole | `tools/test-verdict.sh:121-126` |
| The loss gate's captures are unreadable by its own parser | `peercap.sh` captures on `any`, producing LINUX_SLL2; `lossrate.py` rejects link type 276, so `-w`/`-l`/`-L` collect a capture nothing reads and gate on nothing. Set `AMINETXDUO_PEER_IFACE=ens18` or teach `packets()` the SLL2 header | `tests/perf/lossrate.py`, `tests/perf/peercap.sh` |
| Real X-Surf hardware trails the other stacks; emulation says we lead | Recorded on his hardware: 699 vs 1103 KB/s, worst arm -37% (1.58x; "half" is the verbal report). Emulated xsurf100z2, n=5: we read 477 vs AmiTCP_NG 384 (+24%) and Roadshow 442 (+8%), a2065 control 524/354/444, profile shows no extra byte-walk — not stack architecture. Open hypothesis: ACK-clock starvation on a slow real receiver. Decisive ask: the sign of his 0.21.0 vs 0.21.3 delta | this row carries the means; raw arms `~/anxd-xsurfgap/build/xsg-results.txt` on playhouse3 until swept |
| `whois -6` calls a timeout a refusal | Reports `connection refused` after 191 s, which is the connect ladder running out, not an RST | `src/tools/whois.c` |
| Unpaced `iperf -u` is 8x slower than paced | 1.00 Mbit/s flat out against 8.00 Mbit/s at `-b 8000` with nothing lost. Unchanged by the pacing fix, so it is its own defect | `src/tools/iperfcore.c` |
| `anxnet.device` claims the X-Surf and cannot drive it | The row matches 4626/23 and `attach` fails: the card's DP8390 sits behind ISA Plug-and-Play (`qemuvga/ne2000.cpp`, writes to `0x279*2`, a flags bit at `0x007e`) and nothing configures it, so no unit appears. Refuses cleanly rather than misbehaving. Removing the row renumbers every `ANXNET_UNIT_PIN` after it, so it is a breaking change, not a tidy-up | `src/netdev/netdev_cards.c:35` |
| `anxnet.device` trails `cnet.device` by 5% on the PCMCIA card | Interleaved, n=3: read 434.3 vs 458.3, write 381.7 vs 405.0, spreads ±0.6% and ±0.2%. Our receive path spends 26.8 scalar register accesses a frame at 5.7 us against cnet's ~18, which is ~50 us of a 181 us/frame deficit; the rest is not localised to any measured span. Dropping the settling read after a page switch (5 accesses) measured as a wash and was reverted. No DMA and no 32-bit path exist for this card in Amiberry, so the 750 word port accesses each way are a floor | `src/netdev/dp8390.c`, `src/netdev/ne2000.c` |
| No harness boots Kickstart 2.x with a Zorro network board | `anxnet.device` has only ever run under 3.1. A600 + KS 2.05 + `run-fitzbench.sh` writes no guest transcript at all, and the control proves it is not ours: `a2065.device` fails identically (both rc=124, empty stdout.txt). `run-addifleak.sh` names KS 2.05 but defaults to an A1200 on PCMCIA. What is unproven is the romtag under 2.x and `card.resource` V37 | `tests/tools/run-addifleak.sh`, `tests/perf/run-fitzbench.sh` |
| binutils `amiga-2.46` cannot assemble gcc 16.2's own output | `jne`/`jeq` are MIT pseudo-branches gas sizes itself; 2.46 forces a byte displacement and rejects them -- `Error: invalid byte branch offset`, 2097 of them building libnix. Minimal repro: `m68k-amigaos-gcc -S` on libnix `sources/misc/detach.c`, then `as`. The same file assembles clean under 2.39 (528-byte object). The same 2.46 branch also flipped objdump to MIT syntax and made `strip` refuse a hunk executable carrying HUNK_DEBUG -- all three are one binutils bump | bebbo's `binutils-gdb` `amiga-2.46`; we pin `amiga-2.39.0` |
