# Backlog
What is outstanding. Nothing else — finished work leaves a commit message and a
comment beside the code, not an entry here.
**HARD CAP: 250 LINES.** Over it, delete rows. Never relocate them; git has it.
**ONE LINE PER ROW.** What is wrong and where. Not how it was found.
| Item | Why it is open | Cite |
|---|---|---|
| The playhouse3 Actions runner is offline, so 16 of 26 ci.sh stages never run | `gh api .../actions/runners` reports `playhouse3 status=offline`, and every `Emulator` workflow run since 2026-08-30 went queued then cancelled at the 24h timeout -- including the one for the v0.26.3 release. `tools/check-stage-coverage.sh` now prints which file invokes each stage and 16 of them say `emulator.yml`: rate, bridged, lossgate, cards, cards6, e2e, e2ecards, emulator, smb, fetchtls, matrix, ltoprobe, reachability, tlsloop, wirequiet, capture. The runner is installed at `~/actions-runner` with `run.sh` and `svc.sh` but no service is registered and that box has no passwordless sudo, so `svc.sh install` needs a human; `run.sh` under nohup would work but would seize the rig the perf campaign measures on | `tools/check-stage-coverage.sh:48`, `.github/workflows/emulator.yml:612` |
| `stage_console` is declared and no workflow invokes it | `tools/check-stage-coverage.sh` allowlists it so the gate can be green on the rest, which is a deferral and not a decision. It is tier 2 and wants a real link and a second host, the same rig the emulator tier already has for `bridged` and `lossgate`, so the reason it is unwired is that nobody wired it, not that it cannot be. `stage_rate` was in exactly this state and a 4.3% receive regression shipped through the gap | `tools/check-stage-coverage.sh:27`, `tools/ci.sh:2410` |
| A third build profile, `micro`, below `minimal`, for 512K-1024K machines | there are two today and the option set that defines `minimal` is duplicated in THREE places `tools/check-shipping-config.sh` now holds equal -- `.github/workflows/release.yml:258`, `tools/ci.sh:112` and `dist/make-dist.sh`'s `MINIMAL_OPTIONS` -- so a third profile is three more entries and a CI arm, plus a drawer beside `Libs/minimal/` in the archive with its rows in `install/ARCHIVE-MANIFEST`, plus a third button on the Installer's stack page, which is a radio page and radio pages cannot be driven from a test (first row above). Most of what it wants gone is already an option; **DHCP is not** -- there is no `AMINETXDUO_DHCP`, the client is unconditional, and `ConfigureNetInterface`'s DHCP half and the Installer's DHCP-vs-static page both assume it is there, so that part is new code and a new configuration story for a machine that must be given a static address | `tools/ci.sh:112`, `install/Install-AmiNetXDuo:648` |
| The installer's radio pages cannot be driven, so `-p` and the new layout page are untestable | `install/test/run-workbench.sh -p minimal` reports `stack=full` and FAILS -- correctly, it refuses the vacuous pass. `installdrive.c` posts a synthetic GADGETUP at the option; the Installer does not read the selection from the message. TWO THINGS MEASURED AND RULED OUT on a real Workbench 3.1: (a) `RemoveGList` + flag + `AddGList` reorders the window's gadget list, because AddGList's position `~0` means at-the-end, and the NEXT page then goes unanswered (`installer_card_selected=no stack=unknown`); (b) setting `GFLG_SELECTED` under `Forbid()` plus `RefreshGList` leaves the following pages intact but STILL yields `stack=full`, so the Installer reads the choice from neither the message nor the gadget flag. Next: find how radio_page records its answer -- the Installer's own state, not the gadget -- before writing more driver code | `install/test/installdrive.c:97`, `install/test/run-workbench.sh:93` |
| The installer's GUI drawer choice is reachable but untested | the `askchoice` is drawn at AVERAGE and EXPERT and a user can pick "Into its own drawer" with no marker file. The Installer's radio pages cannot be driven from outside, so `run-workbench.sh -D` takes the same path with `S:AmiNetXDuo-drawer`; that run now stages a foreign stack and checks its libraries, driver, configuration and `AmiTCP:` data remain untouched while the boot selector starts the private stack. Only the literal GUI click remains uncovered; both routes share the implementation from `D_ROOT` onward | `install/Install-AmiNetXDuo:747`, `install/test/installdrive.c:97` |
| The guru after `NetShutdown` is unreproduced | the arm exists now -- `run-hwcard.sh -a shutdown` detaches, re-adds and restarts httpd, then asks ICMP and HTTP separately so `machine_stopped` and `network_did_not_return` are told apart, which one probe cannot do. `amiga-1200.local` answered no ARP at 192.168.1.219; the arm is running against it now. `tools/hwrun.sh -A amiga-1200.local -n` answers `hw_icmp=yes hw_http=yes` and the machine's own clock reports five hours of uptime, so an earlier sweep reporting no 3Com OUI on the segment missed it; that sweep's log was deleted with its worktree, so what went wrong there is not established | `tests/tools/run-hwcard.sh` |
| The CNet16 probe-order fix has a host test but has never met the card | NE2000 register 31 is odd and `ne2000_probe_reset()` strobes it by READING it, which a clone asserting `-IOIS16` unconditionally never sees, so the chip was not reset and `ne2000_detect()` refused it on CR before the word-read probe below could run. Cold boot hides it, warm reboot does not. Amiberry decodes both windows 1:1 and cannot hold a card that refuses a byte read, so the retry never fires there. `test_netdev_ne2000.c` covers the logic -- reverting the fix turns 11 assertions red -- and `run-hwcard.sh -a probeorder` is the run: it claims the card twice in one power cycle and reads `probe_warm_cr_retry`, `probe_warm_cr_last` and `probe_warm_attached`. It needs an A1200 with a CNet16 in the slot; the one on the LAN holds a 3c589 | `src/netdev/ne2000.c:401`, `:438` |
| `pc_settle()`'s own loop is still underived from a measurement | the spins-per-raster-line figure is measured on amiga-1200 twice, 25 and 27 bracketing a power cycle against a floor of 252, so `gayleratio_timed_path_binds=no` on the accelerated machine as well and the accelerator premise inverts -- it is 2.5 us a spin against the 14 MHz reference's 0.25, so the unconditional `us * 4` floor decides every wait and decides it LONG. What is derived rather than measured is `pc_settle` itself: its loop body reads PCMCIA attribute memory, not `$DFF004`, so its constant differs and only the direction carries over | `tests/tools/run-gayleratio.sh:221` |
| Roadshow reads 31 KB/s more than we do on its own `x-surf-100.device` | 949 against 918 on a user's A3000, same card; our `anxnet.device` reads 938 there, so the deficit is iComp's driver and not the card. The lab arm cannot price it: the X-Surf's data port is mirrored across 128 bytes so a `movem.l` costs one Zorro II cycle per longword, and Amiberry answers a longword with two `ne2000_wget` calls, which prices the saving at zero and inverts the ordering | `tools/emu-board.sh`, `src/netdev/netdev_cards.c` |
| The 3c589 RX FIFO-hold fix has no wire number | `run-hwcard.sh -a overruns` reads `netstat -s` either side of a 4 MB receive and emits the deltas; the effect is the difference between two runs with the two libraries at the same load, so one run is only a number. The C mock stays green | `tests/tools/run-hwcard.sh`, `src/netdev/test/test_netdev_el3.c:35` |
