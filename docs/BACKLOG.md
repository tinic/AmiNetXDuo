# Backlog
What is outstanding. Nothing else — finished work leaves a commit message and a
comment beside the code, not an entry here.
**HARD CAP: 250 LINES.** Over it, delete rows. Never relocate them; git has it.
**ONE LINE PER ROW.** What is wrong and where. Not how it was found.
| Item | Why it is open | Cite |
|---|---|---|
| An interface taken down cannot be brought back up | `Offline eth0` takes 15.5 s and orphans the a2065 readers -- `reader 0 did not stop. Its stack leaks. A free here corrupts memory the reader runs on`, then `interface has orphaned readers. It is not restarted` -- so `Online eth0` answers `eth0 did not go online` and the next `GetNetStatus` returns 5. Present in the baseline before today's `netstat -s` leak fix, so it is not that. It did not appear on the `anxnet.device` ClassicWB guest | `src/sana2/sana2_rx.c`, `tests/tools/run-livetools.sh` |
| The guru after `NetShutdown` is unreproduced | the arm exists now -- `run-hwcard.sh -a shutdown` detaches, re-adds and restarts httpd, then asks ICMP and HTTP separately so `machine_stopped` and `network_did_not_return` are told apart, which one probe cannot do. `amiga-1200.local` answered no ARP at 192.168.1.219; the arm is running against it now. `tools/hwrun.sh -A amiga-1200.local -n` answers `hw_icmp=yes hw_http=yes` and the machine's own clock reports five hours of uptime, so an earlier sweep reporting no 3Com OUI on the segment missed it; that sweep's log was deleted with its worktree, so what went wrong there is not established | `tests/tools/run-hwcard.sh` |
| The CNet16 probe-order fix has a host test but has never met the card | NE2000 register 31 is odd and `ne2000_probe_reset()` strobes it by READING it, which a clone asserting `-IOIS16` unconditionally never sees, so the chip was not reset and `ne2000_detect()` refused it on CR before the word-read probe below could run. Cold boot hides it, warm reboot does not. Amiberry decodes both windows 1:1 and cannot hold a card that refuses a byte read, so the retry never fires there. `test_netdev_ne2000.c` covers the logic -- reverting the fix turns 11 assertions red -- and `run-hwcard.sh -a probeorder` is the run: it claims the card twice in one power cycle and reads `probe_warm_cr_retry`, `probe_warm_cr_last` and `probe_warm_attached`. It needs an A1200 with a CNet16 in the slot; the one on the LAN holds a 3c589 | `src/netdev/ne2000.c:365`, `:402` |
| Keystroke latency on truecolour misses the 47 ms requirement | A3000 + ClassicWB P96, 800x600x16, six rounds of 15: mean 51.3 to 70.0 ms and p90 98.9 to 137.7, so 1.1-1.5x the requirement on mean and 2.1-2.9x on p90. 640x480x8 meets it on mean and straddles it on p90. The banded readback is worth 2.9x on mean and 1.6x on p90 against this rig's own baseline, not the 3.7x/4.1x measured on A1200+uaegfx, and p90 is the half the complaint is about | `tests/tools/run-console.sh`, `src/tools/httpfb.c` |
| Four `third_party/threadx` pins in history are reachable from no remote ref | `check-gitlinks.sh` reports `gitlinks_history=FAILED oids=186 unfetchable=4`, and `stage_submodules` aborts on it, so `tools/ci.sh host` cannot run on a lab host at all -- agents have to reproduce the host stage by hand. It survives a full `git fetch --all --tags` in the submodule | `tools/check-gitlinks.sh:221` |
| The v0.25.5 pool and window derivation has no gate | the two 0.25.3 acknowledgment holes are gated by `tcp_persist` and the SWS rule by `test_tcp_sws_host.c`; the pool arithmetic that reopened the window is measured and unguarded | `tests/netstack/host/` |
| The vendored tree carries half an experiment nothing finished | `third_party/netxduo/common/src/nx_tcp_socket_state_data_check.c:1292` is `#if defined(AMINETXDUO_TCP_ACK_CAP_MSS)` and no build in this tree defines it. It is in the pin, an ancestor of `cf3e7642`; the CMake half was on a branch that has now been deleted | `third_party/netxduo/common/src/nx_tcp_socket_state_data_check.c:1292` |
| `stallgaps.py` has no self-test and cannot attribute an ACK clock | the branch it was rewritten from carried `ack_clock_gaps()`, `useful_acks()` and a 70-line `test_stallgaps.py`; the rewrite kept none of them, and the open acknowledgement-latency and Roadshow-read rows are what would use them | `tests/perf/stallgaps.py` |
| `pc_settle(300000)` is unmeasured on the accelerated machine we have | the timed path binds only above `us_per_line * 4` spins per raster line and no emulated arm reaches the ratio, so `gayleratio_timed_path_binds=no` everywhere. **amiga-1200 (192.168.1.219) is a PiStorm32-accelerated A1200 with a 3c589 in the socket, it is on the LAN, and `tools/hwrun.sh` drives it** -- this was never blocked on hardware | `tests/tools/run-gayleratio.sh`, `tools/hwrun.sh` |
| One card arm reported TCP dead in both directions and it has never come back | `tx_bytes=0 rx_bytes=0` with `udp_tx_bytes=748230` on the same boot -- so the interface was online and only TCP was gone. Seven later runs of the same binary passed, including three interleaved arms of three reps, and it could not be attributed. A receive path that loses TCP alone is not noise, and nothing currently would catch it a second time | `tests/tools/run-cardsweep.sh` |
| `AMINETXDUO_GREEN_REALM` costs 8-10% of write and no test has ever run inside it | the realm is live -- 2 baton transitions against the baton build's 84, so the Exec handoffs did become in-realm stack switches -- and read is 0.5-0.7% down for it. What is missing: an account of where the write loss comes from, counters that a shipping build can reach (`netstat`'s `green:` line is inside the RXPROBE-gated budget block), and any ctest at all, because the `green` cross arm is `TESTS=OFF` | `CMakeLists.txt:596`, `tools/ci.sh` |
| `tests/netstack/devs/Internet/name_resolution` still names a SLIRP address | `nameserver 10.0.2.3` exists on no real segment, so on the bridged rig every first lookup goes there, waits about a second and is re-sent -- measured, two packets per name. About 30 harnesses share the file; two override it | `tests/netstack/devs/Internet/name_resolution` |
| Two of the three multifunction cards the CIS walk reaches still have no card row | `CISTPL_LONGLINK_MFC` is followed now and the per-function chains of 3CCFEM556, 3CXEM556 and DP83903 all parse, but only the DP83903 class is drivable: the two 3Com cards' LAN function is an EtherLink III whose MANFID is in no row of `netdev_cards.c`, so `netdev_card_by_cis()` hands them to the NE2000 fallback and they still fail. It needs a card row, `ANXNET_CARD_NAMES` and the manual's card list -- not more CIS work | `src/netdev/netdev_cards.c`, `docs/user/AmiNetXDuo.guide` |
| No host test covers what `BeginIO` does to a request's fields | `netdev_begin_io()` zeroed `ios2_WireError` before dispatch for the life of the driver, and that field is `S2_ONEVENT`'s INPUT mask, so every event request ever issued was refused and the seven events the driver posts had no reachable waiter. `test_netdev_event.c` drives `netdev_event.c` directly and never enters `BeginIO`, which is why it stayed green over it | `src/netdev/netdev_device.c:2012`, `src/netdev/test/test_netdev_event.c` |
| Roadshow reads 31 KB/s more than we do on its own `x-surf-100.device` | 949 against 918 on a user's A3000, same card; our `anxnet.device` reads 938 there, so the deficit is iComp's driver and not the card. The lab arm cannot price it: the X-Surf's data port is mirrored across 128 bytes so a `movem.l` costs one Zorro II cycle per longword, and Amiberry answers a longword with two `ne2000_wget` calls, which prices the saving at zero and inverts the ordering | `tools/emu-board.sh`, `src/netdev/netdev_cards.c` |
| `anxnet.device`'s direct fill produces no checksum, so every frame is walked twice | 5960 direct fills and 0 summed while filling, against `cnet.device`'s 6874 summed -- a counter difference, so rig-independent. The 12 ms acknowledgement gap that goes with it is NOT soundly measurable here and the 2026-08-20 figure has the same defect: anxnet has a 32-bit wide-window PIO path and Amiberry prices exactly that at zero, so anxnet-against-cnet on an emulated NE2000 is driver-against-driver on the one arm where that is unsound | `src/netdev/dp8390.c`, `src/tools/netstat.c:550`, `tools/emu-board.sh` |
| The 3c589 RX FIFO-hold fix has no wire number | `run-hwcard.sh -a overruns` reads `netstat -s` either side of a 4 MB receive and emits the deltas; the effect is the difference between two runs with the two libraries at the same load, so one run is only a number. The C mock stays green | `tests/tools/run-hwcard.sh`, `src/netdev/test/test_netdev_el3.c:35` |
| The TCP window ceiling is unreachable, so its clamp is dead code | `BSD_TCP_WINDOW_CEILING` is `(AMI_POOL_MAX_PACKETS / 8) * AMI_POOL_PAYLOAD` = 100352 and `budget` is the same expression over a pool `AMI_POOL_MAX_PACKETS` caps at 512, so `window = budget / (live + 1)` never exceeds it and the `>` at `socket.c:127` never fires. Measured on an A3000 at 32 and 128 MB: identical, 512 requested and 513 built. Raise the ceiling or delete the clamp -- either is a decision | `src/bsdsocket/socket.c:127`, `src/bsdsocket/bsdsocket_internal.h:195` |
| The toolchain pin cannot move to binutils 2.46, and it is LTO that stops it now | the relocation carve-out is restored and wired -- `build-toolchain.sh` derives the patch directory from the branch in `PINS` and refuses an unrecorded one, and a 2.46 `strip` goes from 0 relocations to 324. What is left is that neither diff in `tools/patches/binutils/` applies to 2.46, so `-DAMINETXDUO_LTO=ON` does not link; that has to be redone against 2.46's own LTO support | `tools/patches/binutils/`, `tools/build-toolchain.sh:355` |
| Payload content is unverified on a real 3c589 | the arm is staged and verified full-length against a live bridged guest -- 27 cases, 29 connections, 0 mismatches -- using `run-payverify.sh`'s own length list, `paysum` and `paypeer.py`. It has never run on the card | `tests/tools/run-hwcard.sh` |
