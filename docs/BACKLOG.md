# Backlog
What is outstanding. Nothing else — finished work leaves a commit message and a
comment beside the code, not an entry here.
**HARD CAP: 250 LINES.** Over it, delete rows. Never relocate them; git has it.
**ONE LINE PER ROW.** What is wrong and where. Not how it was found.
| Item | Why it is open | Cite |
|---|---|---|
| The playhouse3 Actions runner died on 2026-08-27 and 16 of 26 ci.sh stages went with it | RUNNER RESTORED 2026-09-09, STAGES NOT YET PROVEN. The listener lost its broker connection (`_diag/Runner_*.log` ends `[ERR BrokerServer] Catch exception during request`), exited, and the machine's crontab had only an `@reboot` guard, so 25 runs queued against `status=offline` and none ran. It is online again and `tools/runner-keepalive.sh` (cron `*/10`, flock, stamp file) covers a mid-life death; cron was observed advancing the stamp on its own. The oldest queued run went `in_progress` immediately and is executing card sweeps. WHAT IS STILL OPEN: a live runner is not a passing tier. `tools/check-stage-freshness.sh` still reports the sixteen in BASELINE_DEAD and only a COMPLETED run moves one to `revived`; the emulator runs draining now are old commits from before `76e9d182`, so they do not carry the address fix either. Close this row when freshness reports `revived>0`. | `tools/check-stage-freshness.sh`, `.github/workflows/emulator.yml:625` |
| `stage_console` is declared and no workflow invokes it | `tools/check-stage-coverage.sh` allowlists it so the gate can be green on the rest, which is a deferral and not a decision. It is tier 2 and wants a real link and a second host, the same rig the emulator tier already has for `bridged` and `lossgate`, so the reason it is unwired is that nobody wired it, not that it cannot be. `stage_rate` was in exactly this state and a 4.3% receive regression shipped through the gap | `tools/check-stage-coverage.sh:27`, `tools/ci.sh:2505` |
| A third build profile, `micro`, below `minimal`, for 512K-1024K machines | there are two today and the option set that defines `minimal` is duplicated in THREE places `tools/check-shipping-config.sh` now holds equal -- `.github/workflows/release.yml:258`, `tools/ci.sh:113` and `dist/make-dist.sh`'s `MINIMAL_OPTIONS` -- so a third profile is three more entries and a CI arm, plus a drawer beside `Libs/minimal/` in the archive with its rows in `install/ARCHIVE-MANIFEST`, plus a third button on the Installer's stack page, which is a radio page and radio pages cannot be driven from a test (first row above). Most of what it wants gone is already an option; **DHCP is not** -- there is no `AMINETXDUO_DHCP`, the client is unconditional, and `ConfigureNetInterface`'s DHCP half and the Installer's DHCP-vs-static page both assume it is there, so that part is new code and a new configuration story for a machine that must be given a static address | `tools/ci.sh:113`, `install/Install-AmiNetXDuo:648` |
| `-p` selects the option it names, and the drawer layout is answered through its GUI page | RESOLVED 2026-09-08. The radio pages were always drivable and the synthetic GADGETUP always landed; `run-workbench.sh:96-97` mapped the two options BACKWARDS. `Install-AmiNetXDuo:667` lists `(choices "Everything" "Minimal, no IPv6/TLS")` and ids number from 2 in listed order, so 2 is full and 3 is minimal. Measured on the rig: on that page `gadget 2 type=0x1 flags=0x86` -- GFLG_SELECTED already set, it is the default, and the default is MINPICK 0. `-p minimal` now gives `stack_installed=minimal bytes=222844 wb_rc=0`, the first time any run has installed it. GFLG_SELECTED is set alongside the click and both are needed. | `install/test/installdrive.c:97`, `install/test/run-workbench.sh:110` |
| The GUI drawer choice is tested; what remains is that `-D` alone still cannot reach it | RESOLVED 2026-09-08 via `run-workbench.sh -D -g -p drawer`, `wb_rc=0`, which runs the whole self-contained contract with the layout answered from the askchoice. WHY IT WAS NEVER COVERED: `-D` plants `S:AmiNetXDuo-drawer` (`run-workbench.sh:661`), `Install-AmiNetXDuo:747` reads it into FORCE_DRAWER, and `:769` asks the layout question ONLY when FORCE_DRAWER is 0 -- so the flag meant to cover that page was the reason it was skipped. `-g` omits the sentinel and answers the page instead. `stage_e2e` runs it now (`tools/ci.sh:1592`): before that, grepping ci.sh for `run-workbench.sh` found `-H` and NOTHING ELSE, so the whole self-contained contract -- the foreign-stack byte checks at `run-workbench.sh:1146-1303`, the strongest thing this tree asserts about coexisting with Roadshow or AmiTCP -- had no stage at any tier. | `install/Install-AmiNetXDuo:747`, `install/test/installdrive.c:97` |
| The guru after `NetShutdown` is unreproduced | BLOCKED ON HARDWARE THAT NO LONGER EXISTS. `amiga-1200.local` is dead and stays dead; playhouse3 with Amiberry is the rig, permanently. Nothing in this row can be progressed until that changes, and the text below describes a machine that was answering when it was written. the arm exists now -- `run-hwcard.sh -a shutdown` detaches, re-adds and restarts httpd, then asks ICMP and HTTP separately so `machine_stopped` and `network_did_not_return` are told apart, which one probe cannot do. `amiga-1200.local` answered no ARP at 192.168.1.219; the arm is running against it now. `tools/hwrun.sh -A amiga-1200.local -n` answers `hw_icmp=yes hw_http=yes` and the machine's own clock reports five hours of uptime, so an earlier sweep reporting no 3Com OUI on the segment missed it; that sweep's log was deleted with its worktree, so what went wrong there is not established | `tests/tools/run-hwcard.sh` |
| The CNet16 probe-order fix has a host test but has never met the card | BLOCKED ON HARDWARE THAT NO LONGER EXISTS. `amiga-1200.local` is dead and stays dead; playhouse3 with Amiberry is the rig, permanently. Nothing in this row can be progressed until that changes, and the text below describes a machine that was answering when it was written. NE2000 register 31 is odd and `ne2000_probe_reset()` strobes it by READING it, which a clone asserting `-IOIS16` unconditionally never sees, so the chip was not reset and `ne2000_detect()` refused it on CR before the word-read probe below could run. Cold boot hides it, warm reboot does not. Amiberry decodes both windows 1:1 and cannot hold a card that refuses a byte read, so the retry never fires there. `test_netdev_ne2000.c` covers the logic -- reverting the fix turns 11 assertions red -- and `run-hwcard.sh -a probeorder` is the run: it claims the card twice in one power cycle and reads `probe_warm_cr_retry`, `probe_warm_cr_last` and `probe_warm_attached`. It needs an A1200 with a CNet16 in the slot; the one on the LAN holds a 3c589 | `src/netdev/ne2000.c:401`, `:438` |
| `pc_settle()`'s own loop is still underived from a measurement | the spins-per-raster-line figure is measured on amiga-1200 twice, 25 and 27 bracketing a power cycle against a floor of 252, so `gayleratio_timed_path_binds=no` on the accelerated machine as well and the accelerator premise inverts -- it is 2.5 us a spin against the 14 MHz reference's 0.25, so the unconditional `us * 4` floor decides every wait and decides it LONG. What is derived rather than measured is `pc_settle` itself: its loop body reads PCMCIA attribute memory, not `$DFF004`, so its constant differs and only the direction carries over | `tests/tools/run-gayleratio.sh:221` |
| Roadshow reads 31 KB/s more than we do on its own `x-surf-100.device` | 949 against 918 on a user's A3000, same card; our `anxnet.device` reads 938 there, so the deficit is iComp's driver and not the card. The lab arm cannot price it: the X-Surf's data port is mirrored across 128 bytes so a `movem.l` costs one Zorro II cycle per longword, and Amiberry answers a longword with two `ne2000_wget` calls, which prices the saving at zero and inverts the ordering | `tools/emu-board.sh`, `src/netdev/netdev_cards.c` |
| The 3c589 RX FIFO-hold fix has no wire number | BLOCKED ON HARDWARE THAT NO LONGER EXISTS. `amiga-1200.local` is dead and stays dead; playhouse3 with Amiberry is the rig, permanently. Nothing in this row can be progressed until that changes, and the text below describes a machine that was answering when it was written. `run-hwcard.sh -a overruns` reads `netstat -s` either side of a 4 MB receive and emits the deltas; the effect is the difference between two runs with the two libraries at the same load, so one run is only a number. The C mock stays green | `tests/tools/run-hwcard.sh`, `src/netdev/test/test_netdev_el3.c:35` |
