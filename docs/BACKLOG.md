# Backlog
What is outstanding. Nothing else — finished work leaves a commit message and a
comment beside the code, not an entry here.
**HARD CAP: 250 LINES.** Over it, delete rows. Never relocate them; git has it.
**ONE LINE PER ROW.** What is wrong and where. Not how it was found.
| Item | Why it is open | Cite |
|---|---|---|
| ~~The playhouse3 Actions runner died on 2026-08-27 and 16 of 26 ci.sh stages went with it~~ **CLOSED 2026-09-09** | All sixteen are back. `tools/check-stage-freshness.sh` reports `revived=16 dead_known=0` against run 34331957352 (`57259c98`), the first emulator run to conclude success since 2026-08-27, and `BASELINE_DEAD` is empty now so the next stage to die is `DEAD_NEW` and reddens this gate the day it happens. TWO THINGS WERE WRONG, not one: the listener had lost its broker connection and needed `tools/runner-keepalive.sh` (cron `*/10`, flock, stamp), and once it was back the card sweep still failed every run because `cksum(AMINETXDUO_CARDSWEEP_ID) % 6` is a CONSTANT in CI -- the id defaults to `basename $ROOT`, which on the runner is always `AmiNetXDuo`, so every sweep drew slot 5 and a2065 landed on 192.168.1.191, held by a real machine. One failing arm fails the single job. The relocation walk moved it to .241 and all nine cards passed, IPv4 and IPv6. | `tools/check-stage-freshness.sh`, `tests/tools/run-cardsweep.sh` |
| `stage_console` is declared and no workflow invokes it | **IT RUNS, MEASURED 2026-09-09.** This row used to argue that the stage was unwired because nobody wired it and not because it could not be; that was an argument, and it is a measurement now. On playhouse3 against `playhouse2` as the client: `tests/tools/run-console.sh -c playhouse2 -C ham6 -m A1200` gave `CONSOLE_RC=0 RESULT=PASS arms_run=1 ham6_pixels_mismatched=0` -- a HAM6 screen opened on an emulated A1200, was captured, and the client rendered the PNG. The rig was left clean, no amiberry or serial-timestamp surviving. WHAT IS STILL OPEN is only the wiring: no workflow calls it, so it is still allowlisted in `tools/check-stage-coverage.sh` and still proves nothing on a push. Wiring it into emulator.yml is now a change with a known-good stage behind it, not a gamble. `stage_rate` was in exactly this state and a 4.3% receive regression shipped through the gap | `tools/check-stage-coverage.sh:27`, `tests/tools/run-console.sh` |
| A workflow arm whose required variable is unset SKIPS and reports **success**, and nothing counts that as a hole | `tools/ci-arm.sh:78-90`: a `-r VAR` that is empty prints `::warning::`, writes a SKIPPED row and **exits 0**. The step then shows green. MEASURED, not inferred: of the 12 `AMINETXDUO_*` variables that workflow arms declare with `-r`, ten are defined and **two are not** -- `AMINETXDUO_RATE_IFACE` and `AMINETXDUO_RATE_PEER` -- and on emulator run `34331957352` both read empty in the job environment while the step "Throughput against the recorded baseline" concluded `success`. So the throughput gate has never run there. The seven wiring gates cannot see this (they check that a stage is NAMED) and `tools/check-stage-freshness.sh` cannot either (it counts workflow-level `success`), which makes this the eighth question: not "is the stage called" or "did the caller finish" but "did the arm inside actually execute". NOT ACTED ON: enabling those two variables is the throughput gate, and the performance mandate is withdrawn -- that is the repository owner's call, not a side effect of a gate tick. The gate to build is one that fails on an arm reporting `status=skipped reason="... is not set"`, with `rate` on a two-way allowlist so it is green today and fails the day someone sets those variables and forgets the entry | `tools/ci-arm.sh:78`, `tools/check-stage-freshness.sh`, `.github/workflows/emulator.yml:625` |
| `-p` selects the option it names, and the drawer layout is answered through its GUI page | RESOLVED 2026-09-08. The radio pages were always drivable and the synthetic GADGETUP always landed; `run-workbench.sh:96-97` mapped the two options BACKWARDS. `Install-AmiNetXDuo:667` lists `(choices "Everything" "Minimal, no IPv6/TLS")` and ids number from 2 in listed order, so 2 is full and 3 is minimal. Measured on the rig: on that page `gadget 2 type=0x1 flags=0x86` -- GFLG_SELECTED already set, it is the default, and the default is MINPICK 0. `-p minimal` now gives `stack_installed=minimal bytes=222844 wb_rc=0`, the first time any run has installed it. GFLG_SELECTED is set alongside the click and both are needed. | `install/test/installdrive.c:97`, `install/test/run-workbench.sh` |
| The GUI drawer choice is tested; what remains is that `-D` alone still cannot reach it | RESOLVED 2026-09-08 via `run-workbench.sh -D -g -p drawer`, `wb_rc=0`, which runs the whole self-contained contract with the layout answered from the askchoice. WHY IT WAS NEVER COVERED: `-D` plants `S:AmiNetXDuo-drawer` (`run-workbench.sh:661`), `Install-AmiNetXDuo:747` reads it into FORCE_DRAWER, and `:769` asks the layout question ONLY when FORCE_DRAWER is 0 -- so the flag meant to cover that page was the reason it was skipped. `-g` omits the sentinel and answers the page instead. `stage_e2e` runs it now (`tools/ci.sh:1592`): before that, grepping ci.sh for `run-workbench.sh` found `-H` and NOTHING ELSE, so the whole self-contained contract -- the foreign-stack byte checks at `run-workbench.sh:1146-1303`, the strongest thing this tree asserts about coexisting with Roadshow or AmiTCP -- had no stage at any tier. | `install/Install-AmiNetXDuo`, `install/test/installdrive.c:97` |
| The guru after `NetShutdown` is unreproduced | BLOCKED ON HARDWARE THAT NO LONGER EXISTS. `amiga-1200.local` is dead and stays dead; playhouse3 with Amiberry is the rig, permanently. Nothing in this row can be progressed until that changes, and the text below describes a machine that was answering when it was written. the arm exists now -- `run-hwcard.sh -a shutdown` detaches, re-adds and restarts httpd, then asks ICMP and HTTP separately so `machine_stopped` and `network_did_not_return` are told apart, which one probe cannot do. `amiga-1200.local` answered no ARP at 192.168.1.219; the arm is running against it now. `tools/hwrun.sh -A amiga-1200.local -n` answers `hw_icmp=yes hw_http=yes` and the machine's own clock reports five hours of uptime, so an earlier sweep reporting no 3Com OUI on the segment missed it; that sweep's log was deleted with its worktree, so what went wrong there is not established | `tests/tools/run-hwcard.sh` |
| The CNet16 probe-order fix has a host test but has never met the card | BLOCKED ON HARDWARE THAT NO LONGER EXISTS. `amiga-1200.local` is dead and stays dead; playhouse3 with Amiberry is the rig, permanently. Nothing in this row can be progressed until that changes, and the text below describes a machine that was answering when it was written. NE2000 register 31 is odd and `ne2000_probe_reset()` strobes it by READING it, which a clone asserting `-IOIS16` unconditionally never sees, so the chip was not reset and `ne2000_detect()` refused it on CR before the word-read probe below could run. Cold boot hides it, warm reboot does not. Amiberry decodes both windows 1:1 and cannot hold a card that refuses a byte read, so the retry never fires there. `test_netdev_ne2000.c` covers the logic -- reverting the fix turns 11 assertions red -- and `run-hwcard.sh -a probeorder` is the run: it claims the card twice in one power cycle and reads `probe_warm_cr_retry`, `probe_warm_cr_last` and `probe_warm_attached`. It needs an A1200 with a CNet16 in the slot; the one on the LAN holds a 3c589 | `src/netdev/ne2000.c:401`, `:438` |
| `pc_settle()`'s own loop is still underived from a measurement | the spins-per-raster-line figure is measured on amiga-1200 twice, 25 and 27 bracketing a power cycle against a floor of 252, so `gayleratio_timed_path_binds=no` on the accelerated machine as well and the accelerator premise inverts -- it is 2.5 us a spin against the 14 MHz reference's 0.25, so the unconditional `us * 4` floor decides every wait and decides it LONG. What is derived rather than measured is `pc_settle` itself: its loop body reads PCMCIA attribute memory, not `$DFF004`, so its constant differs and only the direction carries over | `tests/tools/run-gayleratio.sh:221` |
| Roadshow reads 31 KB/s more than we do on its own `x-surf-100.device` | 949 against 918 on a user's A3000, same card; our `anxnet.device` reads 938 there, so the deficit is iComp's driver and not the card. The lab arm cannot price it: the X-Surf's data port is mirrored across 128 bytes so a `movem.l` costs one Zorro II cycle per longword, and Amiberry answers a longword with two `ne2000_wget` calls, which prices the saving at zero and inverts the ordering | `tools/emu-board.sh`, `src/netdev/netdev_cards.c` |
| The 3c589 RX FIFO-hold fix has no wire number | BLOCKED ON HARDWARE THAT NO LONGER EXISTS. `amiga-1200.local` is dead and stays dead; playhouse3 with Amiberry is the rig, permanently. Nothing in this row can be progressed until that changes, and the text below describes a machine that was answering when it was written. `run-hwcard.sh -a overruns` reads `netstat -s` either side of a 4 MB receive and emits the deltas; the effect is the difference between two runs with the two libraries at the same load, so one run is only a number. The C mock stays green | `tests/tools/run-hwcard.sh`, `src/netdev/test/test_netdev_el3.c:35` |
| ~~The `xsurf` card sweep fails about 1 arm in 7, and it is NOT the address~~ **CAUSE FOUND 2026-09-10, it was the harness** | The peer's UDP server-arm sender retried with no back-off. A UDP send cannot fail -- there is no connection to refuse -- so `peer_cmd send udp` exits 0 whether or not anything is listening, and the `continue` that ends a successful attempt sat outside the test for it and caught the failing one too, skipping the one-second sleep. The guest does not open 7405 until stage 14 of 15, so the peer fired full payloads at a closed port for the whole run: `cardsweep-xs4-xsurf.pcap` holds 54 bursts of 521 datagrams, 28,128 packets, ~41 MB at 1470 bytes, from 12:15:44 -- before the guest booted -- to the 300 s timeout. The guest takes and drops every one and on an emulated A1200 that is the CPU its TCP stages needed. THREE EARLIER READINGS WERE WRONG: it is not confined to server mode (stage 1 with the guest as CLIENT failed first; the run merely STOPS at the server stage because that is where the harness blocks), inbound was not dead (the guest answered ARP right through the 300 s, the peer's SYN-ACKs went out 8x for 7401 and 7x for 7403 unanswered, and the guest replied to 7 of the 28 ARP requests aimed at it), and the arms did not share an address (SWEEP_SLOT moves address and MAC per sweep; the seven arms drew slots 3,2,0,0,2,1,1 -- four addresses, and only two arms ever ran on .247, so "the same address passed six and failed one" was false). `run-iperf.sh` now backs off and starts the UDP sender behind the TCP one; `tests/tools/peersender-selftest.sh` proves both, and the unfixed loop makes 2,713 attempts in 4 s against the fix's 4. Rows carry `addr=` and `mac=` so this is checkable rather than assumed. WHAT SEPARATED THE SIX FROM THE ONE is not a slow guest -- `AddNetInterface` took 9380-9420 ms on all seven arms including the failing one -- it is whether the peer's socket ever raised `ECONNREFUSED`, which a connected UDP socket only learns from an ICMP port-unreachable and which was the ONLY path that reached the old back-off: xs1 10, xs2 12, xs3 10, **xs4 0**, xs5 11, xs6 11, xs7 11. Six arms were rescued by the guest rejecting the datagrams; the seventh was not, and once the loop span the guest had no room to start. The fix removes the race instead of betting on it. RE-MEASURED 2026-09-10 ON THE FIXED HARNESS: **40 arms, 0 failures** -- xsurf 20 (six slot-indexed, one per `SWEEP_SLOT`, plus 14) at `wall_s` 46-48, a2065 20 at 41-42. Against the old rates (1-in-7 and ~30%) that is p=0.046 and p=0.0008, so a2065's intermittent failures were the SAME flood and its `.191` collision was a separate, CI-only cause. A fixed arm's capture carries ONE burst of ~523 datagrams / 4,382 frames against 54 bursts / 28,128 / 29,583; `srvudp.err` fell 13,030 -> 561 bytes and `ok: the guest served a UDP receive (rc 0)` still passes. STILL OPEN, and it is the stack's not the harness's: why that arm emitted no ICMP port-unreachable at all (it sourced 536 packets all run, essentially just its own UDP blast) -- that one is the stack's, not the harness's. | `tests/tools/run-iperf.sh`, `tests/tools/peersender-selftest.sh`, `tests/tools/cards.sh` |
