# Backlog
What is outstanding. Nothing else — finished work leaves a commit message and a
comment beside the code, not an entry here.
**HARD CAP: 250 LINES.** Over it, delete rows. Never relocate them; git has it.
| Item | Why it is open | Cite |
|---|---|---|
| Two socket files are still outside the host tier, and a wired harness row proves nothing | `a146e614` brought `select.c` and `options.c` in -- 129 checks on the push gate, and the four defects they exist for were each reintroduced and confirmed red. What is left: `tcp_handler.c` is structural, a DOS handler as well as a socket, needing the whole `ACTION_*`/`ERROR_*` vocabulary and `struct FileHandle`; `transfer.c` cannot come in at all, since its static assertions pin `struct iovec`/`struct msghdr` to the 4.4BSD shape against a host with 64-bit pointers. Separately, `tools/check-harnesses.sh` enforces that a harness HAS a runner and that a `manual` row is honest, but nothing enforces that a wired row's runner ever FIRES -- `tests/ipv6/run-socket.sh` names a workflow that is dispatch, cron and tags only | `tests/bsdsocket/CMakeLists.txt`, `tools/check-harnesses.sh` |
| 37 harnesses are UNWIRED, and two of them cannot simply be wired | Lines 88-122 plus 106: 18 tools rows, clients, four library/app, three netstack, two drills, three ipv6, four tls. `:122 run-hangup.sh` says "IT ASSERTS NOTHING TODAY" -- it forwards a status that is now a SKIP, so wiring it would wire a harness that cannot go red, and the assertion has to come first. `:117 run-tools-amiberry.sh` is the only home `nslookup`, `NetTrace`, `traceroute` and `whois` have anywhere, and it needs rewriting off SLIRP before it can run at all. The `manual` rows at 126-157 are holes with a stated cause -- peer, bridged, bench, asset, windows -- and are not part of this | `tests/HARNESSES:88-122` |
| Seven commits carry a submodule OID that was never an object | `3cb2e52e` recorded `third_party/netxduo` at `a5366b1d47d5f00b...`; the only object with that prefix is `a5366b1d73b91c30...`, so the tail was fabricated rather than truncated. `git submodule update` fails at `3cb2e52e`, `03a47b94`, `e24f286e`, `f2b66e2a`, `6d15998c`, `b50e84ab` and `e81bbbc2`. `f61b2dfa` fixed it forward and its message frames it as an abbreviation problem, which it was not. Current main is clean; a bisect landing in that span cannot check out | `f61b2dfa` |
| The fork's own regression suite has never run, on either side | `tools/ci.sh host` does not configure `third_party/netxduo/test/`, and the fork itself has an active `regression_test.yml` whose run count is zero -- `gh api repos/tinic/netxduo/actions/runs --jq '.total_count'` answers `0`. Every regression test added alongside a vendored fix is unexecuted code that reads as coverage | `tools/ci.sh`, `third_party/netxduo/.github/workflows/regression_test.yml` |
| Submodule pins name topic-branch tips instead of merges | `1d8b8a15` moved `third_party/netxduo` from merge `396dc632` back to topic tip `7206b214`, which is not a descendant of it (`merge-base --is-ancestor` answers no), so that commit's tree silently reverts every fork fix merged in between; `b8bb2bc8` has the same shape. Later merges restored a coherent pin, so main is fine. The habit is what matters: it produced a fabricated object id once already. A bump should pin the `master` merge by full 40-character id, with `cat-file -e` and `merge-base --is-ancestor` both checked before committing | `1d8b8a15`, `b8bb2bc8` |
| The first `SetMode()` or `WaitForChar()` of a console session is refused | `term_packet_current()` accepts a packet only if `dp_Port == term_break_port`, or, when that is NULL, if `dp_Port->mp_SigTask == term_shell_task`. `term_shell_task` is assigned at `httpterm.c:1435`, after the first switch that gates `ACTION_SCREEN_MODE` (`:1181`), `ACTION_WAIT_CHAR` (`:1326`) and `Open("*")` (`:1377`), so at session start both are NULL. Separately, a process the Shell spawned has its own `pr_MsgPort`, so those three calls now fail for it with `ERROR_INVALID_LOCK` where they worked before; handles inherited from the Shell still work, since `ACTION_READ`/`WRITE` carry `fh_Arg1` and are not gated | `src/tools/httpterm.c:454`, `:1181`, `:1377`, `:1435` |
| The DHCP/RA absorb runs on the caller's stack with about 680 bytes to spare | `ami_ns_dhcp_search_reconcile` is a 1280-byte frame and `ami_ns_dns_absorb_dhcpv6` 1148, and both run on the calling task's stack (`netstack_dns.c:1092`), reached from `netstack_dns_absorb_pending()` either inside a lookup (`:1370`, `:1400`, `:1806`) or from a live-config read -- `netstack_config()`, `bsd_GetDefaultDomainName`, `bsd_ObtainDomainNameServerList`. They are siblings, so the worst single addition is 1280. `stack_test` measures an ordinary API path touching 2132 of a Shell's 4096, leaving 1964, so 1280 fits with roughly 680 to spare. A MARGIN, not a crash: it only costs anything when a lease or RA event is pending, which makes it intermittent, and nothing measures the two together. Every shipped command linking `toolsock.c` is on this path -- host, nslookup, nc, telnet, traceroute, tftp, whois, sntp, ping, fetch, CensusProbe, NetTrace, NetCapture, iperf, httpd -- plus hostname, netstat, ShowNetStatus, Online/Offline, CheckNetConfig, GetNetStatus and NetSetup through the config door. Only `fetch` swaps its stack | `src/netstack/netstack_dns.c:1092` |
| The 4096-bit fallback is where the crypto stack frame is largest | `c68k_mont_power_modulus()` cannot fit a 4096-bit modulus in its 2048-limb scratch, returns `NX_CRYPTO_SIZE_ERROR`, and `c68k_powm.c:392` falls back to the vendored path, whose 68k fast path puts `HN_UBASE c68k_scratch[400]` plus `c68k_rem[136]` -- 2144 bytes -- in one frame. That is the frame that overran the 4 KB guest stack on ISRG Root X1; it needed 2552 bytes below that point and had 2456. Fixed for guests by `02915948`, but the frame is still the largest in the tree and it is reached from `bsdsocket.library` | `src/crypto68k/c68k_powm.c:392`, `third_party/netxduo/crypto_libraries/src/nx_crypto_huge_number.c:1450` |
| No resolution the lab tests is unaligned, which is why the console served garbage for a year | Every console and RTG arm runs at a width that is a multiple of 16 and whose byte pitch has nothing to pad -- 1024/8 is exactly 128. A user's CyberGraphX 1368x768x8 screen sits in a 1600-pixel bitmap, and until `httprtg.c` took the width from the screen instead of the bitmap the console served 1600 columns: 232 of somebody else's pixels on every frame and 17% more bytes than the screen contains. The fix is in; the hole that hid it is not. An arm at a width that is neither a multiple of 16 nor pitch-aligned is what would have caught it, and nothing in `tests/tools/run-console.sh` can currently ask for one | `tests/tools/run-console.sh`, `src/tools/httprtg.c:621` |
| `nc -l` from outside has never been exercised under Amiberry | `tests/tools/run-nettools.sh` forwards a port with `uae_slirp_redir`, which is FS-UAE's option: Amiberry ignores it and the harness's own probe finds nothing in LISTEN. The repaired verdict names it -- `nettools_nc_inbound=unforwarded` -- rather than failing on it, so the inbound case is honest about being absent instead of quietly green. A bridged guest needs no forwarding at all, which is the shape the fix should take | `tests/tools/run-nettools.sh` |
| Our `telnet` answered no option negotiation the peer recorded | Two real runs against `netpeer.py` logged `telnet answers:` empty and no `client answered` lines, though `src/tools/telnet.c` does implement WONT/DONT. Not gated, because netpeer returns on `quit` mid-buffer and drops the rest, so it may be an ordering artefact of the peer rather than the client. Worth one look with the peer reading to the end | `src/tools/telnet.c`, `tests/tools/netpeer.py` |
| The handshake figures in this file were taken at different emulated clocks | The 100.4 s `www.gnu.org` handshake quoted here does not reproduce: the same three-certificate TLS 1.3 chain verified in 9.1 s at Amiberry's stock A1200 clock and 31.1 s at 7 MHz. The crypto fix is real and separately measured -- 2544 rebinds to 0, ARP probes answered 7 of 24 to 31 of 31 -- but any two timings here are only comparable if the clock is stated with them. `tests/tls/run-reachability.sh` takes `-k` for exactly this reason and defaults to 7 | `docs/BACKLOG.md`, `tests/tls/run-reachability.sh` |
| The crypto yield hook is installed by tls.library alone | `c68k_yield_hook` is set in `tls_netx_bind()`, which only runs on `TLSOpen()`. Anything linking `aminetxduo_tls` directly -- `tls_bench`, `tls_handshake`, `tls_https` today, any guest tool built that way tomorrow -- gets a NULL hook, so `C68K_YIELD()` is a no-op and the baton is held across the whole of the public-key arithmetic. Only the baton holder runs ThreadX code, so the IP thread does not run for the duration. Installing the hook in `tls_https` did NOT make it complete, so this is a real gap and not the cause of that timeout; the two are separate | `src/tlslib/tls_netx.c:130`, `src/crypto68k/crypto68k.h:427` |
| Three TLS harnesses default to SLIRP and cannot pass on this host | `run-https.sh` has no `-B` at all; `AMINETXDUO_AMIBERRY_BACKEND` is the only way to bridge it. This Debian build's SLIRP is a stub that logs `stub, uae_slirp_start` and carries on, so a SLIRP run times out with no result rather than failing. `run-api.sh` and `run-tools-amiberry.sh` are the same shape. SLIRP is banned here anyway | `tests/tls/run-https.sh`, `tests/tls/run-api.sh`, `tests/ipv6/run-tools-amiberry.sh` |
| The expunge refusal is proven at the library and at the port, never at the joint | `bsd_lib_expunge()` declining on `netstack_can_unload()` is `tests/bsdsocket/host/test_expunge_host.c`; `tx_amiga_kernel_stop()` genuinely refusing and genuinely stopping refusing is `tools/smoke/KernelStop`. Nothing tests the lines between them: that `ami_ns_kernel_stop_locked()` leaves `ami_ns_kernel_started` set when the stop fails, so a later `netstack_can_unload()` answers FALSE, and that `netstack_startup()` retrying the stop is what lets a refusal clear. `netstack.c` will not compile on the host -- NetX Duo's linux port types `ULONG` as `unsigned long` against the shim's `unsigned int` -- and no guest can provoke a refusal: the only application `TX_THREAD` that outlives a last close is an orphaned SANA-II reader, which needs a driver that ignores both `AbortIO()` and `S2_OFFLINE`. A test-only SANA-II device that does exactly that is the one route to an end-to-end proof | `src/netstack/netstack.c:1852`, `:2192`, `tests/bsdsocket/host/test_expunge_host.c` |
| The PCMCIA card cannot be given a MAC per run | Amiberry's NE2000 ignores the `mac=` option and takes the host NIC's address -- `3e:24:11:93:e8:8b` in every run of the nine-card sweep -- so the fresh-MAC-per-run rule cannot be honoured on that one board. Two back-to-back runs on it therefore alias in a peer's neighbour cache, which is the confounder the derived MAC exists to remove | `tools/emu-board.sh`, `tests/tools/cards.sh` |
| `C:ssh` is the one artefact that does not reproduce from its own tag | 44 absolute source paths reach the binary through `__FILE__` (libtomcrypt asserts, `src/netio.c`), so the published v0.24.0 ssh is 616 bytes larger than a local rebuild -- exactly 44 times the 14-character difference between the two build directories. Every other file in the archive is byte-identical, including all three libraries. `clients/dropbear/build.sh:181` builds `DB_CFLAGS` with no `-ffile-prefix-map`, and that flag appears nowhere in the tree. Not a content difference: the shipped ssh is the code the tag says it is | `clients/dropbear/build.sh:181` |
| The console pacing does not turn a cheaper pass into a sooner one | With unchanged-band skipping on, the 8-bit arm's duty fell 50% to 24% against a 75% cap while its latency got *worse*, 56.6 to 69.3 ms. There is budget to grab three times as often and it goes unspent, so the skip had to be gated to the banded path instead of helping everywhere. `FB_GRAB_FLOOR` and the `fb_idle_given` accounting -- idle is added to the granted total before the floor raises it -- are where to look | `src/tools/httpfb.c` |
| Two agents on the same model and variant collide on the guest MAC | `tools/classicwb.sh:856` derives the MAC from model and variant alone, so a second A3000/rtg guest boots onto the first one's address. It happened: an agent's guest took the address of one already in use. `AMINETXDUO_CWB_MAC` overrides it, but the default should not collide | `tools/classicwb.sh:856` |
| `rfbbench` cannot price the banded path | Its timed loop calls `rfb_encode_frame`, which always encodes a whole frame; `--bands` only affects the untimed decode-and-compare pass. A measurement of banding taken with it reads the whole-frame path instead, which produced wrong 1.6-1.8x figures once already | `src/rfb/host/rfbbench.c` |
| HAM and EHB are proven on one machine and one resolution | The three modes pass on real emulated A1200s with pixel-exact PNGs, and HAM6, EHB and a mid-session switch into a 64-colour screen and back to Workbench are confirmed by eye. What is left is an A600 for HAM6 and EHB on OCS, and any resolution other than 320x256 lores | `src/tools/httpfb.c`, `tests/tools/run-console.sh` |
| Five things cannot be done to a live IPv6-only interface, and they force a version bump | `ConfigureNetInterface` now applies `GATEWAY6` and refuses `ADDRESS6` and `CONFIGURE6` by name. What is missing under them: a netstack writer for a live global address (the only caller of `nxd_ipv6_address_set()` early-returns on an interface that already has its link-local, and nothing calls `nxd_ipv6_address_delete()`); per-interface DHCPv6 and SLAAC verbs (`ami_netstack_dhcpv6_configure()` returns once `ns_Dhcpv6WorkReady` is set, picks its interface at boot and has no renew entry); a `NETSTATUS_DHCP6` table, so a v6 `TIMEOUT` has nothing to poll; an origin field on `NetStatusAddress6`, so nothing can tell SLAAC from DHCPv6 from `ADDRESS6=`; and a per-index Release verb. Any of those needs a new `NETCTRL` op, which the header's own rule moves `AMI_NETSTATUS_VERSION` 10 to 11 for, with `AMI_NETSTATUS_MIN_REVISION` and `BSD_LIB_REVISION` moving with it -- both checks are exact equality | `src/netstack/netstack_ipv6.c:277`, `src/netstack/netstack_dhcpv6.c:526`, `include/aminetxduo/netstatus.h:67-92`, `:340` |
| No console arm exercises a screen arriving mid-session | `tests/tools/run-console.sh` does one boot per depth and states the depth "is a property of the screen Workbench opens and not of anything this can change while it is running", so every arm connects to a screen already in front and already settled. That is why none of them caught a session dying on a screen that had just arrived. The missing arm is a live session plus a program that opens an 800x600x16 screen and publishes it only some seconds later | `tests/tools/run-console.sh` |
| Keystroke latency is 3.8x the requirement on a truecolour screen | Measured on the A3000, interleaved, 15 samples an arm: 8-bit 46.8-48.3 ms mean against truecolour 174.8-182.6 ms, p90 193-214 ms. The 8-bit arm reproduces the 42.9 ms the banding work was tuned to, so the comparison is sound. The frame is 960000 bytes against 307200, and each band re-resolves and re-locks the screen, which cost +66% CPU per screen when banding landed. Hoisting that out of the band loop, a cheap unchanged-band skip, or the hi/lo byte-plane split the author measured at 3.1x on idle are the three levers | `src/tools/httpfb.c:123`, `src/tools/httprtg.c` |
| A console session that works logs nothing | Only the startup banner is written, with no per-session geometry line, so the log cannot say what a session served or at what depth. The refusal path logs unconditionally now; the success path does not | `src/tools/httpd.c:7090` |
| Two IPv6 harnesses still need SLIRP, one of them genuinely | `tests/ipv6/run-tools-amiberry.sh` pings SLIRP's router at `fe80::2`, asserts `arp` lists it and reasons about `10.0.2.3` as a name server, so it needs rewriting against a real link rather than a different default. `tests/ipv6/run-link.sh` is the lesser case: it leaves `-B` off and inherits `amiberry-run.sh`'s own SLIRP default while driving a real a2065, so it needs a backend decision. SLIRP is never to be used here | `tests/ipv6/run-tools-amiberry.sh`, `tests/ipv6/run-link.sh` |
| DHCPv6 server selection by preference is untested | The vendored client implements OPTION_PREFERENCE (RFC 8415 18.2.1) and no server in the lab can send it: dnsmasq 2.91 refuses the named form, drops the numeric one because our ORO does not ask for option 7, and `dhcp-option-force` appends a second 8192-byte option after which the Advertise never reaches the wire at all. So a link with two DHCPv6 servers picks by arrival order, and `tests/ipv6/run-dhcpv6.sh` exits 4 on a lost race rather than asserting things about another machine's exchange. Deterministic needs kea | `tests/ipv6/run-dhcpv6.sh`, `src/netstack/netstack_dhcpv6.c` |
| The bring-up figure has never been measured on the build that ships | `tests/ipv6/run-bringup.sh` reads `netstack: mark` lines off the serial log, and those only exist in a `-DAMINETXDUO_LOG=ON` build, so the 2483 ms headline is a logging build's number and there is no way to get the shipping binary's. The DHCPv6 Release defect was exactly this shape from the other side -- it worked only when instrumented, and every measurement taken with logging on said it was fine. A timing mark the shipping build also emits, or a measurement that does not need one, would settle both | `tests/ipv6/run-bringup.sh` |
| Eight harnesses document a `-N BOARD` flag that cannot work | The board reaches `tools/amiberry-run.sh`, but `DEVICE=a2065.device` is written into the staged `NetInterfaces/eth0` with no `tools/sana2-stage.sh` call, so asking for any other card reports a product defect that is a rig bug. `run-toolsay.sh` shows the correct shape | `tests/tools/run-apidrill.sh:148`, `run-evpost.sh:133`, `run-ifreadd.sh:147`, `run-mcastrace.sh:159`, `run-multiaddr.sh:203`, `run-srcsel.sh:159`, `run-dns.sh`, `run-routes.sh` |
| The DP8390 PIO cards lose a third of a UDP stream with no Fast RAM | ariadne2, ne2000_pcmcia, xsurf and xsurf100z2 lose 31-34 % of an 852-datagram 2 Mbit stream on a machine with no Fast RAM. The LANCE and ED cards, which have buffer memory of their own, lose 0-2 % on the same run, and no card loses anything with Fast RAM fitted. Measured identically either side of the read-depth change, so it is a property of copying a frame out of a card by hand on a memory-tight machine, not of that change. Nobody has looked at where the frames go | `src/netdev/dp8390.c`, `tests/tools/run-iperf.sh` |
| `tls.library` runs only on our own `bsdsocket.library` | It resolves a socket descriptor to an `NX_TCP_SOCKET *` through a hand-coded `jsr a6@(-864:W)` private LVO, guarded by a `lib_NegSize` check. On Roadshow, AmiTCP or Miami it fails at open with `TLS_ERR_NOSTACK`. Together with the missing `.fd`, this means no third-party application on any other stack can use our TLS at all -- which decides the ceiling on every plan to put it under existing software | `src/tlslib/tls_netx.c:61`, `src/tlslib/tls_conn.c:538` |
| An RSA-PSS-signed certificate cannot be verified | There is no `id-RSASSA-PSS` OID in the parser and no PSS row in the X.509 type set, so a PSS chain link fails closed. PSS works only for TLS 1.3 CertificateVerify. This is an interop limit, not a soundness one, and it will bite as CAs move | `nx_secure_x509_oid_parse.c`, `src/tls/ami_tls_crypto.c:1551-1561` |
| Server-side TLS is 2.8% of the library and unreachable, and cutting it needs a second build | Measured after `--gc-sections`: 8 objects survive, 6492 bytes of a 231608-byte tls.library. The earlier figure of "about 25 translation units" was wrong, and part of what survives is client code sharing those files. It is not latent either -- the server state machine is entered only when `socket_type == SESSION_TYPE_SERVER`, which tls.library never sets, so it is unreachable from network input rather than dormant. The cost of removing it is that the macro changes `NX_SECURE_TLS_SESSION`'s layout, so `tls_handshake`, `tls_decompose` and `tls_interop` -- the only both-ends tests in the tree -- would still compile and fail at runtime. Doing it properly means a second server-enabled nx_secure/aminetxduo_tls pair | `src/tls/CMakeLists.txt` |
| No ALPN, so HTTP/2 cannot be negotiated | The extension does not exist anywhere in nx_secure and nothing in `src/tlslib` or `include/` mentions it. Matters for any browser or client that wants more than HTTP/1.1 | `third_party/netxduo/nx_secure/src`, `include/aminetxduo/tlslib.h` |
| `tls.library` is callable from exactly one compiler | Its stubs are GCC extended asm. There is no `.fd` and no `#pragma libcall` header, so SAS/C and vbcc -- which is every existing Amiga application -- cannot call it without hand-writing pragmas. A shared library only one compiler can call is not one. `fd2pragma` and `fd2sfd` already ship in the pinned toolchain, and `developer/sfd/` already generates the same thing for `aminetxduo_lib`. This blocks every third-party adopter | `include/aminetxduo/tlslib.h`, `developer/sfd/` |
| The on-target source-selection arm cannot separate Rule 6 | `DEVS:NetInterfaces` carries one `ADDRESS6` per interface, so a guest cannot hold a ULA beside its global -- and one link-local plus one global is the configuration in which every 6724 rule agrees. The emulator arm is therefore a wiring gate; the rules are separated only in the host test. A second `ADDRESS6` per interface would make the on-target arm test the rules too | `src/config/config_parse.c:906`, `tests/tools/run-srcsel.sh` |
| `run-fitzbench.sh` cannot see an ACK or window problem | Measured: 1.01 round trips per 32 KB chunk, 37 of 37 samples application-limited, 0 % `rwnd_limited`, advertised window never zero. It is a chunked request/response workload, so it exercises neither the ACK clock nor the receive window. The emulated 477/384/442 comparison in the X-Surf row was taken with it and therefore says nothing about either. A streaming arm (iperf `tcp-rx`) shows the opposite: 100 % `rwnd_limited`, window zero nine times | `tests/perf/run-fitzbench.sh` |
| PCMCIA parity stops short of the CFTABLE walk | Walking every `CISTPL_CFTABLE_ENTRY` instead of taking the first would plausibly reach the seven multifunction ethernet+modem cards `cnet.device` itself cannot drive — inference, needs one such card | `src/netdev/netdev_pcmcia.c` |
| The PCMCIA socket is never contended in the lab | Nothing here holds the socket against us: one `OwnCard()` caller in the guest and no second driver competing for it. So `CARDF_IFAVAILABLE`'s rejection path, and the claim that an initially busy socket is taken on a later `OpenDevice()`, are unexercised. Amiberry does model the Gayle latch (`gayle.cpp:337` acknowledges by writing the mask, `:186` and `:249` deliver the NE2000 interrupt through it) and the V37 acknowledgement now runs on an A600 with Kickstart 2.05, so those two halves of the old caveat are settled. What is left untested here is the socket being held against us, and separately every socket transition -- see the row above | `src/netdev/netdev_pcmcia.c` |
| Amiberry cannot eject a PCMCIA network card, so removal and reinsertion are unexercised | `cfgfile.cpp:6016` sets `inserted` and never clears it -- there is no `else` -- so `uae-configuration ne2000pcmcia_rom_options inserted=false` parses, changes nothing, and returns 0 to the guest. Measured: the interface stayed online and pinged afterwards, and `gayle.cpp:2104` logged no state change. The only code that clears the flag is `pcmcia_eject2()` (`gayle.cpp:2022`), reachable only from a PCMCIA IDE media change; `pcmcia_eject()` is declared and called from nowhere. So `pc_on_removed`/`pc_on_inserted` (`netdev_pcmcia.c:488`, `:511`) have never fired, and the branch's removal path, reinsertion, OwnCard/ReleaseCard ordering and reset-before-release are all unproven. A one-line `else` in Amiberry would make it testable, which is a change to the instrument and its own decision | `netdev_pcmcia.c:488`, `:511` |
| The console records `.pfs` and nothing else can read it | An MP4 export would make a recording shareable. Two constraints decide the codec: the page is self-contained under a CSP that blocks every external fetch, so a muxer must be vendored or avoided; and these are palette screens, where a lossy codec destroys one-pixel text and the export stops being worth having. `VideoEncoder` (WebCodecs) takes per-frame timestamps, which is what the container now carries, so variable frame timing maps straight across. Lossless H.264 4:4:4, or WebM via `MediaRecorder` if the container matters less than the effort. Not for V1 | `src/tools/web/client/console/pfs.ts` |
| CNet and CNet16 still do not work on real hardware | The tier 1-2 parity work shipped in 0.23.0 specifically to drive them, and the reporter says neither attaches while the vendor `cnet16.device` does. So at least one of the five layer fixes or the 16-bit register probe is wrong on hardware Amiberry cannot model. `CheckNetDevice` now names the step each card stopped at, which is the report needed to settle it | `src/netdev/netdev_pcmcia.c`, `src/netdev/ne2000.c` |
| Two card rows have never run on their hardware | `xsurf500` is transcribed from the iComp wiki; `3c589` is written from the 3Com manual and a GPL driver read for facts, with a mock for a test. Both ship dormant and both need one hardware run to become real. What a reporter must send is in the CheckNetDevice guide node | `src/netdev/netdev_cards.c:113,147` |
| Real X-Surf hardware trails the other stacks; emulation says we lead | Recorded on his hardware: 699 vs 1103 KB/s, worst arm -37% (1.58x; "half" is the verbal report). Emulated xsurf100z2, n=5: we read 477 vs AmiTCP_NG 384 (+24%) and Roadshow 442 (+8%), a2065 control 524/354/444, profile shows no extra byte-walk — not stack architecture. Open hypothesis: ACK-clock starvation on a slow real receiver. Decisive ask: the sign of his 0.21.0 vs 0.21.3 delta | this row carries the means; raw arms `~/anxd-xsurfgap/build/xsg-results.txt` on playhouse3 until swept |
| `anxnet.device` trails `cnet.device` by 5% on the PCMCIA card | Interleaved, n=3: read 434.3 vs 458.3, write 381.7 vs 405.0, spreads ±0.6% and ±0.2%. Our receive path spends 26.8 scalar register accesses a frame at 5.7 us against cnet's ~18, which is ~50 us of a 181 us/frame deficit; the rest is not localised to any measured span. Dropping the settling read after a page switch (5 accesses) measured as a wash and was reverted. No DMA and no 32-bit path exist for this card in Amiberry, so the 750 word port accesses each way are a floor | `src/netdev/dp8390.c`, `src/netdev/ne2000.c` |
| No harness boots Kickstart 2.x with a Zorro network board | `anxnet.device` has only ever run under 3.1. A600 + KS 2.05 + `run-fitzbench.sh` writes no guest transcript at all, and the control proves it is not ours: `a2065.device` fails identically (both rc=124, empty stdout.txt). `run-addifleak.sh` names KS 2.05 but defaults to an A1200 on PCMCIA. What is unproven is the romtag under 2.x and `card.resource` V37 | `tests/tools/run-addifleak.sh`, `tests/perf/run-fitzbench.sh` |
| binutils `amiga-2.46` cannot assemble gcc 16.2's own output | `jne`/`jeq` are MIT pseudo-branches gas sizes itself; 2.46 forces a byte displacement and rejects them -- `Error: invalid byte branch offset`, 2097 of them building libnix. Minimal repro: `m68k-amigaos-gcc -S` on libnix `sources/misc/detach.c`, then `as`. The same file assembles clean under 2.39 (528-byte object). The same 2.46 branch also flipped objdump to MIT syntax and made `strip` refuse a hunk executable carrying HUNK_DEBUG -- all three are one binutils bump | bebbo's `binutils-gdb` `amiga-2.46`; we pin `amiga-2.39.0` |
