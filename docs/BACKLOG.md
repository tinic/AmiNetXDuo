# Backlog
What is outstanding. Nothing else — finished work leaves a commit message and a
comment beside the code, not an entry here.
**HARD CAP: 250 LINES.** Over it, delete rows. Never relocate them; git has it.
| Item | Why it is open | Cite |
|---|---|---|
| Keystroke latency is 3.8x the requirement on a truecolour screen | Measured on the A3000, interleaved, 15 samples an arm: 8-bit 46.8-48.3 ms mean against truecolour 174.8-182.6 ms, p90 193-214 ms. The 8-bit arm reproduces the 42.9 ms the banding work was tuned to, so the comparison is sound. The frame is 960000 bytes against 307200, and each band re-resolves and re-locks the screen, which cost +66% CPU per screen when banding landed. Hoisting that out of the band loop, a cheap unchanged-band skip, or the hi/lo byte-plane split the author measured at 3.1x on idle are the three levers | `src/tools/httpfb.c:123`, `src/tools/httprtg.c` |
| Only the 16-bit truecolour path has run on a guest | AWeb's own 800x600x16 screen proves `R5G6B5PC` end to end. 15, 24 and 32-bit have host round-trip coverage and no live session, and the `RGBFB_R8G8B8A8`/`B8G8R8A8` transposition fixed in 49ef2137 is in the 32-bit formats that a 16-bit screen never touches. The guest's UAE config enables `rtg_modes=0x112`, so a 32-bit screen exists to be reached; AWeb ignores its prefs' depth field because the mode ID carries the format, and a guessed 32-bit ID silently fell back to Workbench. Enumerating valid uaegfx mode IDs is what it needs | `src/tools/httprtg.c`, `tests/tools/run-console.sh` |
| A console session that works logs nothing | Only the startup banner is written, with no per-session geometry line, so the log cannot say what a session served or at what depth. The refusal path logs unconditionally now; the success path does not | `src/tools/httpd.c:7090` |
| DHCPv6 server selection by preference is untested | The vendored client implements OPTION_PREFERENCE (RFC 8415 18.2.1) and no server in the lab can send it: dnsmasq 2.91 refuses the named form, drops the numeric one because our ORO does not ask for option 7, and `dhcp-option-force` appends a second 8192-byte option after which the Advertise never reaches the wire at all. So a link with two DHCPv6 servers picks by arrival order, and `tests/ipv6/run-dhcpv6.sh` exits 4 on a lost race rather than asserting things about another machine's exchange. Deterministic needs kea | `tests/ipv6/run-dhcpv6.sh`, `src/netstack/netstack_dhcpv6.c` |
| `ConfigureNetInterface` cannot reconfigure an IPv6-only interface | It is IPv4-only, so the one configuration that now comes up without an IPv4 address cannot be changed in place. `AddNetInterface`, `Online`, `GetNetStatus`, `ShowNetStatus` and `CheckNetConfig` all answer across both families since the IPv6-only work; this command did not follow | `src/tools/configurenetinterface.c` |
| A DHCPv6 fork patch is written and unpushed | `third_party/netxduo` branch `amiga-dhcpv6` (8140f515) holds the vendored client's four defects; the credential on this machine covers `tinic/AmiNetXDuo` and not `tinic/netxduo`, so it 403s. The submodule is deliberately NOT re-pinned and CI is green against the current pin, because the reachable defect is closed from our side by holding `nx_ip_protection`. Patch exported to `anxd-netxduo-dhcpv6.patch` | `third_party/netxduo` |
| The bring-up figure has never been measured on the build that ships | `tests/ipv6/run-bringup.sh` reads `netstack: mark` lines off the serial log, and those only exist in a `-DAMINETXDUO_LOG=ON` build, so the 2483 ms headline is a logging build's number and there is no way to get the shipping binary's. The DHCPv6 Release defect was exactly this shape from the other side -- it worked only when instrumented, and every measurement taken with logging on said it was fine. A timing mark the shipping build also emits, or a measurement that does not need one, would settle both | `tests/ipv6/run-bringup.sh` |
| The tail loss probe resends the oldest outstanding segment, not the newest | `_nx_tcp_socket_loss_probe_check` calls `_nx_tcp_socket_retransmit`, which walks from `nx_tcp_socket_transmit_sent_head` under a one-MSS window. RFC 8985 7.3 asks for unsent data first and otherwise the LAST unacknowledged segment. On a flight of one they are the same packet, which is the request/response shape the probe was added for; on a longer flight it probes what dupacks and the timeout already recover, and leaves the tail -- which is the case 7.2 exists for | `third_party/netxduo/common/src/nx_tcp_fast_periodic_processing.c:150` |
| A tail loss probe throws away what the peer reported | `nx_tcp_socket_retransmit.c:200` clears `nx_tcp_socket_sack_block_count` under RFC 6675 5.1 whenever it is neither a fast retransmit nor in recovery, which is exactly the probe's precondition, and the probe saves and restores four fields and not that one. The next fast retransmit rebuilds the block list from nothing | `third_party/netxduo/common/src/nx_tcp_socket_retransmit.c:200` |
| Without timestamps the round-trip estimate cannot grow past 240 ms | The probe fires at 2*SRTT + 200 ms, which is 240 ms while there is no sample yet, and the probe is a retransmission, so Karn's algorithm abandons the measurement. A path slower than 240 ms is therefore never sampled, SRTT stays where it was, and the timeout guarding it stays on its 1000 ms floor. `rto.drill` is skipped for this and carries the arithmetic | `third_party/netxduo/common/src/nx_tcp_socket_retransmit.c:115`, `tests/tcpdrill/scripts/rto.drill` |
| Eight harnesses document a `-N BOARD` flag that cannot work | The board reaches `tools/amiberry-run.sh`, but `DEVICE=a2065.device` is written into the staged `NetInterfaces/eth0` with no `tools/sana2-stage.sh` call, so asking for any other card reports a product defect that is a rig bug. `run-toolsay.sh` shows the correct shape | `tests/tools/run-apidrill.sh:148`, `run-evpost.sh:133`, `run-ifreadd.sh:147`, `run-mcastrace.sh:159`, `run-multiaddr.sh:203`, `run-srcsel.sh:159`, `run-dns.sh`, `run-routes.sh` |
| Three guest programs return RETURN_OK whatever they measured | The harness forwards that status as its verdict, so a run with confirmed data corruption exits 0. `tests/stress/run-fitzstress.sh` prints DIRTY counts it does not gate on | `tests/compare/checkrunner.c:275`, `tests/soak/fitz_soak.c:1749`, `tests/stress/fitzstress.c:1464` |
| `tests/tools/run-nettools.sh` asserts nothing | No grep, no counter, no check of any kind: it ends `RC=$?` / `exit "$RC"`, so the verdict is the guest's raw return code, and ToolsSmoke returns 0 whatever the commands did | `tests/tools/run-nettools.sh:295`, `:302` |
| An interface with only IPv6 on it is refused | `CONFIGURE=NONE` parses to `AMI_IPTYPE_STATIC` (`config_parse.c:477`) and static with `address == 0` raises `AMI_CFG_PROBLEM_ERROR` (`:1007`), so an interface carrying an `ADDRESS6` and no `ADDRESS` does not come up. That is the machine a managed+other-stateful router describes, and it is also the plain IPv6-only case someone configures by hand. Independent of DHCPv6: even a static IPv6-only interface cannot be expressed today | `src/config/config_parse.c:477`, `:1007` |
| The DP8390 PIO cards lose a third of a UDP stream with no Fast RAM | ariadne2, ne2000_pcmcia, xsurf and xsurf100z2 lose 31-34 % of an 852-datagram 2 Mbit stream on a machine with no Fast RAM. The LANCE and ED cards, which have buffer memory of their own, lose 0-2 % on the same run, and no card loses anything with Fast RAM fitted. Measured identically either side of the read-depth change, so it is a property of copying a frame out of a card by hand on a memory-tight machine, not of that change. Nobody has looked at where the frames go | `src/netdev/dp8390.c`, `tests/tools/run-iperf.sh` |
| `tls.library` runs only on our own `bsdsocket.library` | It resolves a socket descriptor to an `NX_TCP_SOCKET *` through a hand-coded `jsr a6@(-864:W)` private LVO, guarded by a `lib_NegSize` check. On Roadshow, AmiTCP or Miami it fails at open with `TLS_ERR_NOSTACK`. Together with the missing `.fd`, this means no third-party application on any other stack can use our TLS at all -- which decides the ceiling on every plan to put it under existing software | `src/tlslib/tls_netx.c:61`, `src/tlslib/tls_conn.c:538` |
| `renegotiation_info` is never sent on the initial ClientHello | `nx_secure_tls_session_create_ext.c:947-959` clears `renegotation_enabled` whenever TLS 1.3 is live, which it always is here, and the ClientHello only emits 0xFF01 when that flag is already set. So a TLS 1.2 server sees a client that never advertised RFC 5746, and no SCSV goes either. The flag is set only after the server declines 1.3. The library never renegotiates by design, so defining `NX_SECURE_TLS_DISABLE_SECURE_RENEGOTIATION` closes the automatic HelloRequest path at no cost | `nx_secure_tls_send_clienthello_extensions.c:161` |
| Leaf keyUsage is never checked | The verification path parses keyUsage only to test `keyCertSign` on issuers. A leaf whose keyUsage forbids digitalSignature is accepted. The bit constants exist and are never read | `nx_secure_x509_certificate_verify.c:104-114`, `nx_secure_x509.h:288-292` |
| An RSA-PSS-signed certificate cannot be verified | There is no `id-RSASSA-PSS` OID in the parser and no PSS row in the X.509 type set, so a PSS chain link fails closed. PSS works only for TLS 1.3 CertificateVerify. This is an interop limit, not a soundness one, and it will bite as CAs move | `nx_secure_x509_oid_parse.c`, `src/tls/ami_tls_crypto.c:1551-1561` |
| Server-side TLS is compiled in and unreachable | `NX_SECURE_TLS_SERVER_DISABLED` is unset, so about 25 translation units of server handshake state machine link in, while nothing in `src/` ever creates a server session and the public API has no server entry point. Defining it removes the code and the attack surface. `--gc-sections` mitigates the size but not the surface | `src/tls/CMakeLists.txt:63`, `nx_secure_tls_session_create_ext.c:970-973` |
| No ALPN, so HTTP/2 cannot be negotiated | The extension does not exist anywhere in nx_secure and nothing in `src/tlslib` or `include/` mentions it. Matters for any browser or client that wants more than HTTP/1.1 | `nx_secure/src`, `include/aminetxduo/tlslib.h` |
| Two non-forward-secret suites are still offered | `RSA_AES_256_CBC_SHA256` and `RSA_AES_128_CBC_SHA256` use RSA key transport, so a recovered server key decrypts recorded sessions. They sit at the bottom of the offer order and exist for reach | `src/tls/ami_tls_crypto.c:1600-1601` |
| `tls.library` is callable from exactly one compiler | Its stubs are GCC extended asm. There is no `.fd` and no `#pragma libcall` header, so SAS/C and vbcc -- which is every existing Amiga application -- cannot call it without hand-writing pragmas. A shared library only one compiler can call is not one. `fd2pragma` and `fd2sfd` already ship in the pinned toolchain, and `developer/sfd/` already generates the same thing for `aminetxduo_lib`. This blocks every third-party adopter | `include/aminetxduo/tlslib.h`, `developer/sfd/` |
| `fetch.c` says encrypted connections need a 68020 | The 68000 archive carries a working `tls.library` -- one binary, `AMINETXDUO_CPU=any`, 243,180 bytes on every machine -- and an A600 completed a verified TLS 1.2 handshake with it. The comment describes a state that no longer holds | `src/tools/fetch.c` |
| The on-target source-selection arm cannot separate Rule 6 | `DEVS:NetInterfaces` carries one `ADDRESS6` per interface, so a guest cannot hold a ULA beside its global -- and one link-local plus one global is the configuration in which every 6724 rule agrees. The emulator arm is therefore a wiring gate; the rules are separated only in the host test. A second `ADDRESS6` per interface would make the on-target arm test the rules too | `src/config/config_parse.c:906`, `tests/tools/run-srcsel.sh` |
| `run-fitzbench.sh` cannot see an ACK or window problem | Measured: 1.01 round trips per 32 KB chunk, 37 of 37 samples application-limited, 0 % `rwnd_limited`, advertised window never zero. It is a chunked request/response workload, so it exercises neither the ACK clock nor the receive window. The emulated 477/384/442 comparison in the X-Surf row was taken with it and therefore says nothing about either. A streaming arm (iperf `tcp-rx`) shows the opposite: 100 % `rwnd_limited`, window zero nine times | `tests/perf/run-fitzbench.sh` |
| PCMCIA parity stops short of two things | Walking every `CISTPL_CFTABLE_ENTRY` instead of taking the first would plausibly reach the seven multifunction ethernet+modem cards `cnet.device` itself cannot drive — inference, needs one such card. The interrupt work (status-change handler, `CARDF_POSTSTATUS` on V39, the Gayle edge-latch clear on V37, and whether COR bit 6 should request level mode at all) must land as one change and cannot be tested here: Amiberry models neither the Gayle latch nor a contended socket | `src/netdev/netdev_pcmcia.c`, `src/netdev/dp8390.c` |
| The console records `.pfs` and nothing else can read it | An MP4 export would make a recording shareable. Two constraints decide the codec: the page is self-contained under a CSP that blocks every external fetch, so a muxer must be vendored or avoided; and these are palette screens, where a lossy codec destroys one-pixel text and the export stops being worth having. `VideoEncoder` (WebCodecs) takes per-frame timestamps, which is what the container now carries, so variable frame timing maps straight across. Lossless H.264 4:4:4, or WebM via `MediaRecorder` if the container matters less than the effort. Not for V1 | `src/tools/web/client/console/pfs.ts` |
| CNet and CNet16 still do not work on real hardware | The tier 1-2 parity work shipped in 0.23.0 specifically to drive them, and the reporter says neither attaches while the vendor `cnet16.device` does. So at least one of the five layer fixes or the 16-bit register probe is wrong on hardware Amiberry cannot model. `CheckNetDevice` now names the step each card stopped at, which is the report needed to settle it | `src/netdev/netdev_pcmcia.c`, `src/netdev/ne2000.c` |
| Two card rows have never run on their hardware | `xsurf500` is transcribed from the iComp wiki; `3c589` is written from the 3Com manual and a GPL driver read for facts, with a mock for a test. Both ship dormant and both need one hardware run to become real. What a reporter must send is in the CheckNetDevice guide node | `src/netdev/netdev_cards.c:113,147` |
| Real X-Surf hardware trails the other stacks; emulation says we lead | Recorded on his hardware: 699 vs 1103 KB/s, worst arm -37% (1.58x; "half" is the verbal report). Emulated xsurf100z2, n=5: we read 477 vs AmiTCP_NG 384 (+24%) and Roadshow 442 (+8%), a2065 control 524/354/444, profile shows no extra byte-walk — not stack architecture. Open hypothesis: ACK-clock starvation on a slow real receiver. Decisive ask: the sign of his 0.21.0 vs 0.21.3 delta | this row carries the means; raw arms `~/anxd-xsurfgap/build/xsg-results.txt` on playhouse3 until swept |
| `anxnet.device` trails `cnet.device` by 5% on the PCMCIA card | Interleaved, n=3: read 434.3 vs 458.3, write 381.7 vs 405.0, spreads ±0.6% and ±0.2%. Our receive path spends 26.8 scalar register accesses a frame at 5.7 us against cnet's ~18, which is ~50 us of a 181 us/frame deficit; the rest is not localised to any measured span. Dropping the settling read after a page switch (5 accesses) measured as a wash and was reverted. No DMA and no 32-bit path exist for this card in Amiberry, so the 750 word port accesses each way are a floor | `src/netdev/dp8390.c`, `src/netdev/ne2000.c` |
| No harness boots Kickstart 2.x with a Zorro network board | `anxnet.device` has only ever run under 3.1. A600 + KS 2.05 + `run-fitzbench.sh` writes no guest transcript at all, and the control proves it is not ours: `a2065.device` fails identically (both rc=124, empty stdout.txt). `run-addifleak.sh` names KS 2.05 but defaults to an A1200 on PCMCIA. What is unproven is the romtag under 2.x and `card.resource` V37 | `tests/tools/run-addifleak.sh`, `tests/perf/run-fitzbench.sh` |
| The ReadMe that ships still lists neither the score nor five of the things installed | `dist/make-dist.sh:409` copies `dist/ReadMe` to the archive root, so it is the first file a user opens. It says `141 of 142` where README.md says 142, and its install and uninstall lists omit `iperf`, `CheckNetDevice`, `ConfigureNetInterface`, `hostname` and `anxnet.device`. Separately `docs/user/ReadMe` is a second, divergent user ReadMe that ships nowhere and lists six commands nothing installs: `dist/make-dist.sh:474-477` copies only `*.guide`, `*.txt` and `*.doc` | `dist/ReadMe:227`, `docs/user/ReadMe` |
| An IPv6 harness still defaults to SLIRP | `tests/ipv6/run-socket.sh` documents SLIRP as its backend, "which is what the test wants: it talks to ::1 and needs no LAN". The shipped example was corrected in f44f87e1; this one is behaviour rather than text, so it was left | `tests/ipv6/run-socket.sh` |
| Three figures disagree with themselves | The minimal stack is "98 KB" in the Installer help and "about 104 KB" in the comment beside it. `fetch.c` assumes a 4 MB machine where `docs/RESEARCH.md` records the floor as 1 MB and notes six source comments still say 4 MB. `httpterm.c`'s `sock_number()` rejects above 9999, which is five digits, where the comment said four | `install/Install-AmiNetXDuo`, `src/tools/fetch.c:1007`, `src/tools/httpterm.c` |
| Two comments describe something the code does not do | `netdev_cards.h` counted the bridge struct's fields wrongly, so the count was dropped rather than corrected; `NetdevIsaPnp` has five. `toolchain-resolve-selftest.sh`'s `resolve()` says it prints the diagnostic on fd 3, where the code redirects fd 2 to `$TMP/err.txt` | `src/netdev/netdev_cards.h`, `tools/toolchain-resolve-selftest.sh` |
| One guide transcript carries a placeholder | `ping`'s send-failure line reads `cannot send the request: <reason>`. The send path moved from raw NX to sockets, so the errno string was never captured. One emulator run settles it | `docs/user/AmiNetXDuo.guide` |
| binutils `amiga-2.46` cannot assemble gcc 16.2's own output | `jne`/`jeq` are MIT pseudo-branches gas sizes itself; 2.46 forces a byte displacement and rejects them -- `Error: invalid byte branch offset`, 2097 of them building libnix. Minimal repro: `m68k-amigaos-gcc -S` on libnix `sources/misc/detach.c`, then `as`. The same file assembles clean under 2.39 (528-byte object). The same 2.46 branch also flipped objdump to MIT syntax and made `strip` refuse a hunk executable carrying HUNK_DEBUG -- all three are one binutils bump | bebbo's `binutils-gdb` `amiga-2.46`; we pin `amiga-2.39.0` |
