# Backlog
What is outstanding. Nothing else — finished work leaves a commit message and a
comment beside the code, not an entry here.
**HARD CAP: 250 LINES.** Over it, delete rows. Never relocate them; git has it.
**ONE LINE PER ROW.** What is wrong and where. Not how it was found.
| Item | Why it is open | Cite |
|---|---|---|
| NetX drops merged CONTINUES heads at the window edge: with 15-frame runs ~9 TCP packets per 12 s are "dropped on receipt" and the sender retransmits 25-30; with runs of 2, none | `nx_tcp_socket_packet_process.c`'s acceptance against `rx_window_current` versus the window the last ACK advertised, or the fork's settle/ramp; the GENET shows the same key without PSH now | `docs/plans/zz9000-ethernet.md` #8, `src/netdev/netdev_verify.c` |
| The ZZ9000 console offload's firmware service is not built | The host side is complete: httpd auto-detects `zz9k.library` + the `0x8200` service, hands the card the encoder geometry, skips the Zorro readback, and falls back cleanly when the service is absent. The `0x8200` handler (rfb_encode over `MAP_FRAMEBUFFER_SURFACE`) is firmware, not an archive artifact -- a reference implementation is in the design doc; build it into ZZ9000OS (Zynq toolchain) and validate on the A3000 | `docs/plans/zz9000-console-offload.md`, `src/tools/httpzz.c`, `src/tools/httpfb.c` |
| `stage_console` runs (`run-console.sh -c playhouse2 -C ham6 -m A1200` gave `RESULT=PASS`) and no workflow invokes it | still allowlisted in `tools/check-stage-coverage.sh`, so it proves nothing on a push | `tools/ci.sh`, `.github/workflows/emulator.yml` |
| A workflow arm whose `-r` variable is unset SKIPS and reports success | `AMINETXDUO_RATE_IFACE` and `AMINETXDUO_RATE_PEER` are undefined in CI, so the throughput gate has never run there and the step shows green; the wiring gates check that a stage is named, not that it ran | `tools/ci-arm.sh:78-90` |
| The CNet16 probe-order fix has a host test and has never met the card | Amiberry decodes both windows 1:1 and cannot hold a card that refuses a byte read, so only `run-hwcard.sh -a probeorder` on the real A1200 exercises the retry | `src/netdev/ne2000.c:402`, `src/netdev/test/test_netdev_ne2000.c` |
| `pc_settle()`'s own loop is still underived from a measurement | the spins-per-raster-line figure is measured (25 and 27 against a floor of 252); `pc_settle` reads PCMCIA attribute memory, not `$DFF004`, so its constant differs and only the direction carries over | `tests/tools/run-gayleratio.sh:221` |
| Roadshow reads 31 KB/s more than we do on its own `x-surf-100.device` | 949 against 918 on a user's A3000; our `anxnet.device` reads 938 there. The lab cannot price it: Amiberry answers a longword with two `ne2000_wget` calls, which inverts the ordering | `tools/emu-board.sh`, `src/netdev/netdev_cards.c` |
| The 3c589 RX FIFO-hold fix has no wire number | `run-hwcard.sh -a overruns` reads `netstat -s` either side of a 4 MB receive; the effect is the difference between two runs with the two libraries at the same load, on the real A1200 | `tests/tools/run-hwcard.sh`, `src/netdev/test/test_netdev_el3.c:35` |
| The fork changes have never been offered to eclipse-threadx | 247 non-merge commits and 16,897 lines of shipping code diverge from `upstream/dev`; the survey, the tranche order and the per-PR cost are measured, the scope decision is not taken | `docs/UPSTREAMING.md` |
| `WaitSelect()` loses the caller's signals on the EINTR and ENETDOWN exits: bits taken by `SetSignal()`/`Wait()` are neither written to `*sigmp` nor re-raised, only the normal exit writes them | AmiTCP/Roadshow contract: `*sigmp` reports what arrived; an IDCMP or port signal that lands in the same `Wait()` as the break mask is gone | `src/bsdsocket/select.c:824`, `src/bsdsocket/select.c:929` |
| `OpenDevice()` can block (`nd_PcmciaLock`, card.resource claim, core attach) before `lib_OpenCnt++`, which sits at the end | Exec's Forbid() around OpenDevice ends at the first Wait(); a memory-purge expunge in that window frees the device under the running Open; the standard skeleton increments first | `src/netdev/netdev_device.c:2233`, `src/netdev/netdev_device.c:2451` |
| anxnet.device `SetFunction()`-patches exec `ColdReboot` (LVO -726) to stop bus masters before a reboot | exec V47 (OS 3.2) has `AddResetCallback()`; the patch is only needed below V47, and a later patcher pins the device resident | `src/netdev/netdev_reset.c:110` |
| The task-alive test walks `SysBase->TaskReady`/`TaskWait` under Disable(), in the library and in httpd | execbase.h marks the lists private; the conventional form keeps no raw `struct Task *` past its owner's exit (a notify list cleaned at CloseLibrary) | `src/bsdsocket/library.c:211`, `src/tools/httpterm.c:1118` |
| `TDNestCnt`/`IDNestCnt` are read directly (offset hard-wired and static-asserted) to decide interrupt state and whether logging may Wait() | private ExecBase fields | `port/threadx-amiga/src/tx_thread_interrupt_control.c:28`, `src/bsdsocket/loghook.c:94` |
| Green-realm coroutines switch SP by hand; the realm Task's `tc_SPLower`/`tc_SPUpper` describe the realm stack while a green thread runs on its own, and "must not block in Exec" is enforced by convention plus probe builds | `StackSwap()` keeps Exec's view honest; `tool_stackprobe` and debuggers read the wrong stack; a Wait() from a green thread sleeps the whole realm | `port/threadx-amiga/src/tx_green_switch.S:23` |
| The GENET poller (Exec priority -128) runs the service pass and the openers' hooks under Disable() in task context; foreign openers are excluded only because Roadshow hung twice under it | a driver must not run a stack's hooks in a context the stack did not agree to | `src/netdev/genet.c:164` |
| The MDIO link poll (25 us typical, 2 ms timeout) runs inside the Disable()d service pass every 50 blanks | 2 ms worst case with interrupts off | `src/netdev/genet.c:380`, `src/netdev/genet.c:532` |
| `ANXD_S2IOF_L4_CSUM` is a private bit (0x10) in `io_Flags`, a struct SANA-II owns | the versioned extension record could carry the per-request checksum ask instead | `include/aminetxduo/anxs2ext.h:17` |
| `S2_CONFIGINTERFACE` accepts any `ios2_SrcAddr` (multicast bit, all-zero) and programs it | SANA-II: `S2ERR_BAD_ADDRESS`/`S2WERR_SRC_ADDRESS` | `src/netdev/netdev_cmds.c:531` |
| `S2_ONEVENT` queues any event mask; bits the device can never raise are never answered | SANA-II: `S2ERR_NOT_SUPPORTED`/`S2WERR_BAD_EVENT`; a client waiting on such an event waits forever | `src/netdev/netdev_event.c:63` |
| `ReleaseSocket()` refuses a listening socket with EOPNOTSUPP | AmiTCP and Roadshow release any socket; a superserver handing a listener to a child is refused | `src/bsdsocket/handoff.c:243` |
| Application code binds ThreadX/NetX internals: 134 `_tx_*`/`_nx_*` symbols in 42 files (`_tx_thread_system_state++`, `_tx_thread_current_ptr`, `_nx_ip_route_find`, `_nx_secure_tls_*`) | the `nx_`/`tx_` API is the vendor boundary; the rfc7905/alpn files are deliberate extensions, the rest is reach-through, and every NetX Duo/ThreadX upgrade is a re-audit of 42 files | `src/netstack/netstack_baton.c:298`, `src/tlslib/tls_conn.c`, `src/tls/ami_tls_crypto.c` |
| `src/common/crashguard.c` is a dead subsystem: a `tc_TrapCode` handler that RTEs into a bailout with a rewritten PC plus an Alert-vector patch, installed by nothing, and its 68000 group-0 frame layout is wrong | LTO drops it from the shipped library; the source and its `used` attribute remain | `src/common/crashguard.c:47` |
| `sana2_device.h` carries its own copy of the SANA-II constants instead of `<devices/sana2.h>` | drift from the NDK header | `src/sana2/sana2_device.h:149` |
| Functions over 250 lines: `netdev_perform` 558, `pc_configure_owned` 470, `ami_sana2_driver_entry` 296, `ami_sana2_open` 289, `ami_ns_configure_addresses` 284; files over 2000: `httpd.c` 7909, `netstack.c` 4315, `httpfb.c` 3288, `sana2_rx.c` 2744, `socket.c` 2722, `netdev_device.c` 2704, `genet.c` 2138 | one concern per function and file | `src/netdev/netdev_cmds.c:410`, `src/netdev/netdev_pcmcia.c:360` |
| Driver state that is per unit lives in file-scope statics: ~30 profiling counters in the shell, the clock calibration, the dtree base and GIC user count, `zz_cores[2]` | a second card overwrites the first's numbers; a third ZZ9000 has no core | `src/netdev/netdev_device.c:368`, `src/netdev/zz9000.c:205` |
| `genet.c` is one 2138-line core stitched from NetBSD register sequences, Linux PHY timing, the FreeBSD checksum-block layout and 18 Emu68 measurement notes, with profiling scaffolding inline | provenance and concerns are not separated | `src/netdev/genet.c` |
| `io_Unit` is `&op->op_Unit`, a per-opener struct: two openers of one unit see different `io_Unit` values | SANA-II/Exec: `io_Unit` identifies the unit | `src/netdev/netdev_device.c:2448` |
